# 更新日志

本文件记录用户可见的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 维护约定：**每次发版必须更新本文件**（见 [docs/MAINTENANCE.md](docs/MAINTENANCE.md) 的 checklist）。
> 版本号的唯一事实源是 `config.APP_VERSION`。

---

## [1.1.7] — 2026-08-21

### 改进

- **技能文件夹入口更顺手** —— Agent 页「专属资料库」卡片把「打开技能文件夹」放到「打开资料库文件夹」右侧，两个按钮保持同一格式；不必再滚动到工作流说明处才能打开技能目录。
- **少年司法初稿的公开说明统一到 v6** —— 清理应用内 Agent 指引、README 与 MCP 接入说明里残留的决策树、七类原型、两张骨架卡和已停用手册表述，统一为“完整精读原文 → 参照三大刊实测结构搭骨架 → 展示逐字标题树与逐章计划 → 无异议继续全文”的当前流程。

## [1.1.6] — 2026-08-21

### 新增

- **少年司法论文初稿工作流多了一种交付形态：施工蓝图** —— 跟它说「写蓝图」「施工图」，它就**不写全文**，改交一份施工蓝图：全文骨架、每一节要写什么（回答哪个问题、用什么材料、得出什么判断）、每一处引证的完整出处，全都列齐——只差用语言把它组织出来。
  > 这份蓝图是**给别的 AI 用的**：开头写着「请根据本施工蓝图完成论文初稿，以 DOCX 格式交付」，随后是一整节格式要求（数字用阿拉伯数字及五类中文例外、重复引用照写完整题录不写「前引」、正文不写他人姓名、引号一律全角）。你把它整份发出去就行，接手的 AI 不需要再问你什么。
  > 之所以要求这么细，是因为接手的那个 AI **没有你的知识库、也调不了任何工具**：凡蓝图里没写的，它就只能编。所以蓝图里的引证一律写到完整题录与印刷页码，**绝不出现只有你本机才认得的文献编号**——那种编号发出去就是一串乱码，而它多半不会说「我查不到」。

### 改进

- **正文引号一律全角，取消原先「我国台湾地区法规名用直角引号」的例外。** 同时修掉了规则自身的一处自相矛盾：那条规则写着「一律全角」，而它给出的示例用的却是半角引号，照着抄就会抄错。

## [1.1.5] — 2026-08-20

### 新增

- **顶栏多了「🔎 核验工具」** —— 里面两个标签页：「引文核对」给一句引文，回答它在不在原文、位于第几页；「论断核验」给一句话，回答库里有没有证据支持。
  > 这两件事本来只有接上 AI 助手才用得到，界面上没有入口。现在不装任何东西也能直接试——比如随手编一句引文丢进去，它会明确告诉你「未在原文中找到」，而不是含糊其辞。
  > 综述页里原有的「🔎 核验一句话」保留不变，两处用的是同一套判断。
- **运行日志** —— 每次工具调用会记一行到 `data/logs/task_log.jsonl`：任务编号、调用了什么、耗时多久、成功还是出错、这次用掉几次模型调用。运行 `python app/task_log.py` 可以打印一张按工具汇总的表。
  > 日志里**不存你输入的原文**，只存长度和一段哈希——检索词和论断可能涉及研究隐私，而日志恰恰是最容易被整份发出去的东西。

### 改进

- **论断核验会指出「关键处对不上」** —— 此前只要库里有一段和你这句话高度相似的原文，就会判成「有证据支持」。问题是：一句把数字写错的话，和正确的原文除了数字之外几乎一模一样。
  > 现在遇到**数值不一致**或**肯否相反**，结果会降级成琥珀色警示并写明差异，例如：「论断称『3年、5年』，而原文为『1年』，数值不一致，请以原文为准」。
  > 判定文案也从「库内证据支持」改成了**「库内找到支持性原文」**——它证明的是库里有这么一段原文，不是这句话就对了。真伪仍然要你比对证据栏里的原文自行判断。
  > ⚠️ 已知限制：这个检查覆盖不了「某某制度已经建立」这类**存在性断言**，那种错话仍会被判成找到支持性原文。请始终看证据栏。

### 修复

- **把整个 PaperPiggy 文件夹搬到别的盘之后，文件夹模式的库会变空** —— 受管文件夹的位置是按绝对路径记的，文件夹一挪，那个路径就失效了，库看起来像被清空。这与「数据和程序在同一个文件夹里、整个搬走就能用」的设计承诺是矛盾的。
  > 现在路径失效时会自动回落到数据目录旁边的「论文库」。**不会改写你的设置**——外置盘重新接回来时，仍然走你原本配置的那个路径。（Zotero 模式不受影响。）
- **受管文件夹没设置时，会把当前工作目录整个扫进库** —— 空路径被当成了「当前目录」，实测扫出过 430 个无关文件，程序自己的说明文档都被算成了文献。现已挡掉。

### 其它

- 深索新增 `LOCALKB_DEEP_OCR=off` 开关（默认行为不变）。批量建索引时，个别几十 MB 的扫描版专著单篇 OCR 可能耗时几十分钟，把整批拖住。

## [1.1.4] — 2026-08-13

### 新增

- **项目记忆改为分四类存放，一个研究主题一份档案** —— 记忆原本是 `记忆/项目记忆.md` 一个文件，一路往下追加。用久了它会失控（实际使用中涨到过 669 行、七万多字），于是有两个毛病：一是每次开工都要整份读一遍，大到超过 AI 客户端单次读取上限，得分四次才读得完；二是里面把「你的长期偏好」「工具怎么用」「各个题目的结论」「历史流水账」混在一起。
  > 第二条才是真问题：当你说「这篇推倒重来，以我的新文件为准，旧结论作废」时，旧题目的结论仍然会被塞进 AI 的上下文，它**看得见就躲不开**——实测出现过拿既往结论当本轮判断依据的情况。
  > 现在 `记忆/` 下分成四份：`项目记忆.md`（长期偏好、固定规则、已定决策、各主题一行状态）、`工具经验.md`（跟研究题目无关的作业经验）、`主题档案/<主题名>.md`（**一个研究主题一份**，装该主题的结论与待核项）、`变更日志.md`（流水账）。
  > **主题档案的正文默认不进 AI 的上下文**，它要用哪个主题才单独去取。所以「旧结论作废」这次是**物理上读不到**，不是靠嘱咐它别用。
- **主题档案带状态行** —— 每份档案首行标着 `进行中` / `已完成` / `已作废`，作废的会连日期和原因一起写上。AI 可以只改这一行而不动正文。
- **检索可以排除「综合层」** —— 起草类任务要求「只从原始文献出发重新检索」时，现在能让结果里只剩一手文献与法规原文，不掺 AI 自己以前写的综合页。此前只有「只查文献」和「只查法规」两档，想「两样都要、但不要综合页」做不到。

### 改进

- **AI 写记忆写错地方会被拒绝，不再是嘴上劝** —— 项目记忆有 200 行上限、单条 600 字上限，超了写不进去，并直接告诉它该写哪一份；给同一个研究主题另起一份档案会被查重挡回既有那份。此前这些只写在模板的说明文字里，结果就是那 669 行。
- **记忆写入改为纯追加** —— 原来是「读全文 → 拼接 → 整个写回」，多个 AI 会话同时写会丢更新，在 OneDrive 目录下还可能撞上文件被短暂占用而写失败。现已改为直接追加并带重试。

## [1.1.3] — 2026-08-11

### 改进

- **界面不再写死 `127.0.0.1:8770`** —— Agent 页的服务状态栏改为显示实际地址，「接口说明书」链接改为相对路径。此前若换了端口，界面会给出一个连不上的地址。
- **端口可通过环境变量更改**（开发用）—— `LOCALKB_PORT` / `LOCALKB_HOST` 可覆盖默认的 `127.0.0.1:8770`，非法值会告警并回退。这样在正式版常驻运行时，也能另起一个实例调试而不互相抢端口。日常使用不受任何影响：不设这两个变量时行为与以前完全一致。

## [1.1.2] — 2026-08-11

### 修复

- **升级后首次启动弹「后台服务未能启动」** —— 1.1.1 的缓存失效指纹用的是目录文件的**修改时间**，而安装器覆盖 `app/` 时这些文件全部重写、修改时间必变，于是**每次升级都会触发全库重算**，哪怕目录内容一个字节都没改。重算是纯 CPU 的紧循环，实测 42.8 秒里把主线程挤得答不上话，启动器等不到后端响应就报了这个框。
  > 实际上后台一直算得好好的，算完也正常落了盘——**再启动一次就恢复正常**。但这个提示会让人以为应用坏了。
  > 两处都修了：① 指纹改成按目录**内容**算（覆盖安装若内容没变就不再重算）；② 重算过程每 64 篇主动让出一次执行权。实测冷算 33 秒期间，主线程被阻塞最长 **0.07 秒**（启动器的超时是 2 秒），全程 0 次超时。

## [1.1.1] — 2026-08-11

### 修复

- **升级后「库总览」的四档分布不更新** —— 1.1.0 给逐刊缓存加了「定档规则一变就整份重算」，却漏了**分布缓存**：它的失效条件只有题录文件和手动改档文件的修改时间，期刊定档规则变了它一概不失效。于是升级后文献卡上的档位是新的、检索排序也是新的，唯独库总览那张图还是升级前的旧值，**而且重启多少次都不会自愈**（题录没变，缓存永远命中）。
  > 实测：界面显示 权威482/顶级540/核心191/普通217，同一份数据用同一份代码现算却是 548/526/161/195，差 66 篇。现在分布缓存与逐刊缓存共用同一把失效钥匙。
  > 已装 1.1.0 的用户：升级到本版后首次启动会自动重算；不升级的话，手动删掉 `data/grading_dist.json` 再重开应用也能恢复。
  > 影响面仅限这张总览图——**检索排序、文献卡档位、期刊权重加成都不受影响**，它们走的是逐刊缓存，1.1.0 起就已正确。

## [1.1.0] — 2026-08-11

> 本版有**行为变更**（期刊四档分布、研究工作流流程），故进 minor 位而非补丁位。

> **升级后的必要操作**：无需重建索引，也无需手工删任何缓存。本次改了期刊识别与目录数据，**首次启动会自动重算全库期刊档位**（纯离线，不联网、不花钱，通常几十秒内完成）；期间「库总览」的四档分布可能短暂显示为旧值，刷新即可。若你之前手工改过 `data/journals/` 下的目录文件，改动会被保留并同样触发重算。

### 修复

- **同一本刊不再分裂成两个档位** —— 识别期刊时 ISSN 一命中就停，而法学核心（CLSCI）、顶尖法评、精选外文权威、台湾法学这四份名单里的条目没登记 ISSN、只登记了刊名，导致**题录里带 ISSN 的文献整条错过这些名单**。同一本刊会因为某几篇题录恰好有 ISSN 而被判成两个档。现在 ISSN 命中后会并入同名刊的名单信息。
  > 实测（1430 篇真实题录）：《中国社会科学》30 篇原本 28 篇顶级 / 2 篇权威 → 现在 30 篇全部权威；`Criminology` 10 篇、`Asian Journal of Criminology` 18 篇同样从「一半核心一半权威」归位。

### 变更

- **期刊四档重新拉开距离** —— 出厂的标签映射表原本把 SSCI Q1/Q2 直接抬成「权威」、Q3/Q4 抬成「顶级」、CSSCI 抬成「顶级」，与分级引擎自己的判定差 1—3 档，结果 74.5% 的文献挤进最高两档，而四档换算成检索加成只差 0.024——期刊权重事实上不再区分。现改为跟随引擎判定。
  > 实测变化（1430 篇真实题录，三项改动叠加后）：最高两档 71.4% → **41.8%**。中文法学三大刊（法学研究、中国法学、中国社会科学）仍在权威档；外文高分区刊回落到顶级/核心；《法律适用》这类实务刊回到核心。原设置仍可在「库总览 → 评价规则」里逐项改回。

### 新增

- **可以自己裁定某本刊属于哪一档** —— 新增私有名单 `field_focus`：研究者的核心领域刊未必进得了 CLSCI/CSSCI，却是每天要读的。它**不冒充任何官方名录**，只表达"在我的研究方向上，这本刊就该是这一档"。**优先于一切名录（含 CLSCI），能升也能降**——把一本权威刊裁定为普通档同样有效。档位直接写四档名，正式名与娱乐显示名两套都认（权威=夯、核心=人上人、普通=NPC）。出厂预置《青少年犯罪问题》为顶级、《预防青少年犯罪研究》为核心——此前这两本刊连同其余本领域刊全部兜底为「普通」（加成 0.075），而一本神经影像方法刊拿 0.300。
  > 要增删：把 `catalogs/field_focus.json` 复制到 `<数据目录>/journals/field_focus.json` 再改，改完重启应用即可——逐刊缓存会因名单变化自动重算，不用手工删。⚠️ 档名写错（比如写成"重点"）不会报错，只会落到兜底档，改完请回界面核对一眼。

- **研究工作流开始记《检索发现日志》** —— 四条研究工作流每次检索都要留一行：查询词逐字、这条结果排第几、采用还是弃用、为什么。弃用理由尤其重要。另单列一节记"漏召回"——靠脚注追踪或人工翻找才补上、而检索从没返回过的文献。日志随成品落在 `0_Agent交付物/<主题>/检索发现日志.md`。
  > 为什么要记：应用本身**不保存任何检索历史**（检索与问答都不写盘，界面上的"最近检索"只存 15 条且关窗即失），所以"哪个问题该返回哪几篇"这类判断一直在被丢掉。有了这份日志，检索质量才有据可校——以前想校准得回头考古，现在边做边攒。

### 改进

- **出骨架卡之前先读文献，不再凭题目猜结构** —— 第一闸门前新增「选型前侦查」：先建 20—40 条题录的候选池，从中挑 **6—10 篇**核心：完整精读 2—3 篇（材料最新的、最被引或最对立的），其余只读标题树＋引言结论；另核现行法版本时点（不计入篇数）。**读满仍定不下结构就停下来**把卡住的那一问交给你，不靠继续读拖时间。侦查只回答七个问题（有没有通行名称／现场几派在争／条文与试点是否成批／有没有明确对手学说／列不列得出 ≥4 处空缺／哪块材料最新最密／有没有人已经写过），产出一页侦查表与两张骨架卡一起交给你。
  > **边界**：侦查读的是局部，**不产生可引用论断**。正文里每一个判断仍必须走完整精读并核到印刷页——这条是防止用局部阅读伪造页码的底线。
  > 原因：旧流程把完整精读放在选型之后，等于让 AI 看着题目猜该分几章。
- **骨架卡除了提纲，还要写"准备怎么写"** —— 新增一栏「每章、每节准备写什么」：每章 2—3 句、每节一句，逐条交代**本章回答哪个问题｜用哪类材料回答（规范／理论／事实，并注明手上有没有）｜准备得出什么判断**。判据是遮住标题仍能认出是哪一章，"介绍……的基本情况""对……进行分析"一律视为没写；有节的还要写出每节区别于同章其他节的那一点，写不出就合并。两张卡若各章准备写的内容雷同，等于只有一条路线。
  《论文初稿（通用暂用版）》同步跟进：路线诊断前先做同样刻度的小规模摸底，三级提纲逐部分附同格式的内容简述。
- **少年司法初稿的骨架卡不再限于七类原型** —— 决策树每一问都补齐了出口，末端指向新的第九张卡「自建骨架」；选型后还要对候选原型逐个做准入自检（判据不成立、与本题材料的顺序冲突、命中不适用警告、写不出该型的三项打死条件，任一命中即出局）。题目套不进七类原型时自建骨架与套用原型**地位相同**，两张卡可以是「原型＋原型」「原型＋自建」「自建＋自建」。自建卡要额外交代像哪两型、差在哪，以及偏离了哪几条结构基准、依据哪份材料；证据要求、两个用户闸门、硬失败清单与成稿自检**一条不减**。
  > 原因：旧决策树的最后一问「不能 → 回类⑤」把出口封死，任何题目最终都会被分派到某一型，工作流里写着的「不好归类就自建」永远触发不了 —— 表现就是硬套。七类原型归纳自一位作者、一个部门法的 36 篇论文，样本内部本就有归不进任何一类的篇目。
- **四条研究工作流开工前会问你要不要派子代理** —— 逐环节给出默认建议并说明理由（批量精读文献、两张骨架卡各自隔离起草、定稿前独立校核建议派；正文分节起草默认不派，因为章际衔接、章首装置不重复等是跨章约束，并行起草必然打破），**你回答前它不会自作主张地派**；你说"你自己做"就全程不派，标准不变。子代理交回的是材料而不是成稿，闸门与自检不得转包。

## [1.0.53] — 2026-08-08

> **升级后的必要操作**：无需重建已有文献索引。更新后请让 Claude、Codex 或其他 MCP 客户端重新连接一次，以加载 `add_statute` 的 `summary` 参数。已入库法规和既有检索摘要不会被改写；需要补摘要的旧法规仍可调用 `submit_agent_summaries`。

### 改进

- **法规入库可同时提交检索摘要** —— `add_statute` 新增可选 `summary`；摘要在预检阶段即复用现有质量闸门，并与正文、版本元数据一起绑定确认令牌。确认后摘要先写入统一摘要库，再随法规条文嵌入；未提交时明确提示后续调用 `submit_agent_summaries`，`deep_status` 也会说明缺失摘要中有多少篇法规。重复入库不会覆盖既有摘要。

## [1.0.52] — 2026-08-08

### 新增

- **独立本地法规库** —— Agent 可把从官方网页取得并核对的完整法律法规、司法解释原文写入 `data/statutes`；写入前必须先通过域名、正文结构、版本字段与哈希校验，再向用户展示预览并携确认令牌二次调用。法规不会写入 Zotero，与普通文献统一建索引、检索和 Wiki 来源核验，并可按条读取。
- **法规检索范围与版本追踪** —— 检索可限定为全部、普通文献或法规；同名法规按施行/修订日期判定现行版本，旧版本标记为已修订，并返回受版本状态变化影响的 Wiki 反向引用。

### 修复

- **法学引注补齐作者角色与出版项** —— 译者不再丢失，主编按“主编”显示；两位作者全部列出、三位以上仅列首位加“等”；书籍和书章补齐版次格式，法规引注使用版本标签和条号，期刊缺期号时不再伪造“待补期号”正文。

> **升级后的必要操作**：无需重建已有文献索引。更新后请让 Claude、Codex 或其他 MCP 客户端重新连接一次，以加载新的法规工具；只有实际添加法规时，应用才会为该法规增量建立条文索引。

## [1.0.51] — 2026-08-04

### 改进

- **项目记忆成为跨 Agent 的强制接力点** —— Claude Code、Codex 等客户端每次新建 MCP 会话后，必须先完整读取 `0_Agent资料库/记忆/项目记忆.md`；成功读取前，PaperPiggy 会拒绝其他知识库工具，避免 Agent 只看到启动提示中的片段就直接工作。
- **长期信息统一回到项目目录** —— Agent 可以继续使用客户端自己的 auto memory 或私有记忆，但长期偏好、已定决策、关键进度与事实必须同步写入项目记忆，确保换 Agent 后仍能接手；项目根 `AGENTS.md`、`CLAUDE.md` 与项目记忆模板使用同一条最高优先级规则。
- **四处用户指引同步更新** —— 应用内新手指引、应用内 Agent 指引、主仓库 README 与独立教程都补充了项目记忆规则；同时把少年司法初稿的过期“两路线方案卡”表述更新为当前的七类结构原型与两张骨架卡流程。

### 修复

- **读取失败不会误解锁** —— 项目记忆不存在、无法读取或工具调用失败时，会话仍保持锁定；只有成功读取完整文件后才能继续调用其他知识库能力。

> **升级后的必要操作**：无需重建索引。更新后请让 Claude、Codex 或其他 MCP 客户端重新连接一次；新会话会先要求完整读取项目记忆。本版本不会改写已经由用户编辑过的项目记忆正文。

## [1.0.50] — 2026-08-03

### 改进

- **Agent 资料库恢复为人类可读的主资料区** —— 模板升级产生的待合并版本不再以 `.new.md`、`.new.2.md` 散落在技能、参考格式和说明文件旁；统一按原位置层级放进 `0_Agent资料库/升级与备份/待合并/`。
- **所有 Agent 模板备份集中收纳** —— 自动升级恢复件、人工采用新版前的原文件、旧工作流迁移快照，以及安装目录中的 `AGENTS.md` / `CLAUDE.md` 备份，统一按原因和日期放进 `升级与备份/历史备份/`，保留清晰原文件名且不自动删除。
- **新增通俗维护说明** —— `升级与备份/说明.md` 明确区分当前生效文件、待合并新版、历史恢复件和完整数据备份；完整备份会连同这一区域一起保存。

### 修复

- **旧版技术文件会安全归位** —— 首次启动自动迁移既有 `.new*.md`、`.user-backup-*`、`.agent-ws-migration-backup-*` 和资料库根的内部状态 JSON；逐项失败时保留原路径或集中恢复件，不以整理目录为由删除唯一副本。
- **跨盘工作区也能完成整理** —— 旧 Agent 工作区与 `data/state` 不在同一盘符时，内部状态改用独占复制和恢复件保护，不再因跨盘改名失败而长期滞留在资料库根目录。
- **并发保存保护继续生效** —— 用户或编辑器在模板校验、迁移、改名后才完成保存时，用户内容仍优先；集中待合并文件和历史备份均使用独占路径，不覆盖既有合并笔记。

> **升级后的必要操作**：无需重建索引。启动新版一次即可自动整理旧 Agent 工作区；正式安装目录由应用升级时处理，本次发布不会直接改写用户当前安装。

## [1.0.49] — 2026-08-03

### 新增

- **少年司法论文初稿改用结构原型与选型决策树** —— 原先的“教义学／理论—制度建构”两条固定主路线**已废除**。改为按五问决策树，从七类结构原型（上户口型、编格子型、定性辨异型、拆将就办法型、立论型、清点型、换轨型）中选出两类，提交两张骨架卡。废除的依据是 28 篇可判定样本的实测：能被旧二分法干净归类的仅 6 篇，且“纯教义学”一格为空；三组样本的章数中位数同为 6，组内差数倍于组间差，该分类对“文章分几章、每章做什么”没有预测力。
- **骨架卡取代方案卡** —— 卡上要交的不再是“核心问题／中心观点／三级提纲”这类描述，而是可直接落笔的成品：逐字一级标题树、各章字数占比、引言末句的多动词路线图原句、每章章首第一句原句、本型的不可调换处、会打死本型的三项材料条件。
- **第二闸门改为骨架落位表** —— 逐章列明“本章交付的名词”与“下一章章首要点名的名词”，两者须逐字咬合；并须抄出“删掉本章后失去指涉的那一句”具体句子，写不出即判该章为伪功能。
- **成文技艺手册全面重写** —— 提供选型决策树、八张附真实论文逐字标题树的骨架卡、按场合编排的句型库、标题词组池、少年司法 12 条专门约束、不得挪用的自造命名表与可数的成稿自检表。全部结构阈值来自 36 篇中国大陆刑事诉讼法论文（2001—2026）的逐篇实测。

### 改进

- **事实命题与规范命题分开管** —— 事实命题（谁签署、比例多少、条文怎么写）仍严格服从证据身份，二手转述不得升格为事实描述；规范命题与理论命题（某项安排是否自洽、制度应当如何）由概念区分与逻辑推演支撑，**不以数据或条文授权为前提，缺少实证材料不再是降低论断强度的理由**。两者须在文字上分得开，读者要能一眼区分“现行法就是这样规定的”与“现行法应当改成这样”。
- **新增书面语层与工作流术语防泄漏** —— 正文一律学术书面语，列出口语词替换方向并给出六类特征判据（口语疑问式、动词补语式否定、“家”作机关量词、临时比喻、自贬式限定、拟人化）；“选边／格／落位／闸门／工序”等内部作业用语不得出现在论文正文。
- **完成标准全部改为可数判据** —— 18 项逐项报数字（不分节一级单元占比、标题密度、单节承载、章际回指次数、段末回收句数、设问位置等），不报形容词；硬失败条款附可 grep 的机器判据。
- **少年司法论文初稿最终固定交付 DOCX** —— Markdown 只作中间稿、骨架卡、落位表和核验记录；最终成稿必须是可正常打开并经过标题层级、脚注/尾注、分页和基本版式检查的 `.docx`，无法生成或验证时不得虚报完成。
- **研究工作流按“任务类型 → 研究领域”路由** —— 出厂研究工作流固定为“论文初稿（少年司法版）”“综述（少年司法版）”“论文初稿（通用暂用版）”“综述”，另保留“维护综述库”“跨学科发散与补文献”两条支持工作流。
- **专题初稿训练方法分成可靠性与成文技艺两层** —— 今后的专题初稿须完成成文总卡、章节功能图和论证动作卡的 schema 完整性检查，并进行选路前／选路后的正文级前向验证。
- **通用初稿明确适用边界** —— 从少年司法版抽去领域专门规则后作为暂用流程，并始终提示“尚未经过其他部门法训练验证”。**通用版仍沿用“教义学／理论—制度建构”的方法二分**：七类结构原型归纳自刑事诉讼法一个部门法的样本，未经其他部门法验证，不宜直接外推。
- **旧工作流安全迁移** —— 未修改的出厂 `写论文与综述.md` 自动迁入新的 `综述.md`；用户改过的旧文件改名保留，已有目标文件不会被覆盖。

### 修复

- **模板和旁本首次创建增加并发保护** —— 用户或另一进程同时创建工作流、手册或 `.new.md` 时，用户文件优先，出厂内容改存新的受保护旁本。
- **模板校验后的迟到保存不再被覆盖** —— 历史出厂主文件升级前先原子移入可恢复的 `user-backup`，再独占创建新版；任何已存在的 `.new.md` 都只读不改，新版递增另存；改名后的读取或写入失败会先恢复主路径，覆盖检查、真正写入和异常回滚之间的窗口。
- **旧工作流迁移不再删除最终校验后的保存** —— 迁移先把旧入口原子移入不参与顶层列表的私有恢复目录，再独占创建目标或可见保留件；同名保留件并发出现时不会被覆盖，迁移失败会恢复原入口。
- **工作流空白修改不再被误判为出厂原样** —— 升级与迁移改用保留 Markdown 缩进、行尾空格和空行语义的精确历史指纹；旧的去空白指纹继续保留兼容信息，但不再单独授权删除或覆盖。

## [1.0.48] — 2026-07-27

### 修复

- **Zotero 书籍章节、编辑者与机构作者不再混作普通作者** —— 题录现在按原顺序保留 creator 的角色和机构身份；书籍章节、编辑文集、报告、学位论文和期刊文章分别使用适用的引注字段与缺失提示。
- **书籍章节不再错误要求期号** —— 章节引注区分章节作者、编者、书名、出版地、出版社、年份和章节页码；报告可正确输出机构作者与报告编号。

### 改进

- **写作和维护工作流按既有综述主题归类** —— 新增或更新综述时优先充实现有主题页并完成互链，维护结束前复核主题归属，避免页面散乱。
- **用户交付文件不再暴露内部文献 key** —— key 仍可用于 Agent 内部核验和综述库来源追溯；论文、报告、表格、参考文献和维护报告统一使用人类可读引注。

> **升级后的必要操作**：只需运行一次“手动更新知识库”刷新题录；无需清空索引、重新深索或重建向量。

## [1.0.47] — 2026-07-26

### 修复

- **应用升级不再把普通代码改动误报成“必须重建索引”** —— 题录、全文切块和语义向量改用稳定产物契约判断兼容性；错误提示、重试机制、日志等实现变化不会再触发昂贵重建。
- **v1.0.46 的索引提示会自动消除** —— 已有题录、深索、向量和 BM25 均保持有效；升级后会自动把经过审计的历史指纹迁移到稳定契约，不修改文献或综述正文，也不调用嵌入 API。
- **不同索引规则不会被静默混用** —— 未知旧索引不再被直接登记成当前版；切块或向量契约确实不兼容时，增量写入会停止并显示具体原因。

### 改进

- **发布前强制审计索引兼容性** —— 受监控的索引实现发生变化时，构建会要求开发者明确登记“契约不变”的理由或提升契约版本；没有做出迁移判断就不能打包。
- **升级提示说明真实原因** —— 需要人工处理时直接展示具体契约差异，不再只有笼统的“索引由旧规则生成”。

> **升级方式**：在设置页使用“应用更新”，或下载完整安装器覆盖安装。升级后无需清空索引、重新深索或重新调用向量接口；原有索引会自动登记为兼容契约。

## [1.0.46] — 2026-07-26

### 修复

- **Claude、Codex 与其他 MCP 客户端都能完整读取原文** —— `read_source` 的正文统一走 MCP 标准文本通道，不再因客户端优先显示结构化结果而只剩页码和字数。
- **长篇综述可以正常进入语义检索** —— 超长 Wiki 页会按全文分段嵌入，再聚合为页面向量；此前因输入过长缺失的页面会在下次检索加载时自动补齐，无需清空或重建文献索引。
- **嵌入接口报错更容易判断** —— 对远端输入增加长度保护，400 错误会保留安全的服务端原因，能区分输入过长、模型名或参数问题。

### 改进

- **MCP 契约升级并兼容新旧客户端** —— 服务端会协商 `2024-11-05`、`2025-03-26`、`2025-06-18` 与当前 `2025-11-25`：旧客户端继续收到标准文本和兼容工具定义，新客户端获得工具标题、读写注解、输出结构及严格参数错误。
- **40 个工具统一受契约测试保护** —— 自动检查工具名、参数结构、分派入口和结构化输出，避免某个客户端或单个工具静默漂移。
- **Agent 可安全整理综述主题** —— 新增 `set_wiki_theme`，只修改主题归类元数据，不触碰正文、引用或人工核验状态；传空主题可恢复自动归类。

> **升级方式**：在设置页使用“应用更新”，或下载完整安装器覆盖安装。升级后请让 Claude、Codex 或其他 MCP 客户端重新连接一次，以重新载入工具契约。本版不修改文献或综述正文，也不需要重建知识库索引。

## [1.0.45] — 2026-07-25

### 新增

- **已核验综述不再被 Agent 直接改写** —— Agent 更新已核验页时，PaperPiggy 会保存为独立待审稿；原核验版继续显示并参与检索，核验状态不会因 Agent 提交而消失。
- **可以看清 Agent 改了哪里** —— 综述页会列出标题变化、来源与互链增删，并显示逐行正文差异；可选择“接受并核验”“先编辑待审稿”或“放弃待审修改”。
- **核验与待审处理进入版本历史** —— 人工核验、Agent 提交待审稿、接受或放弃修改都会留痕；待审记录与当前核验版在历史中明确区分。

### 修复

- **写入身份由服务端判定** —— Agent 更新正文或互链时不能再通过请求字段伪装成人工操作；单独修改已核验页的互链也会进入待审稿。
- **检索始终使用已发布版本** —— 待审正文、来源和互链不写入正式索引；只有用户接受或编辑保存后，才发布为新的已核验版本。

> **升级方式**：在设置页使用“应用更新”。本版会自动升级综述库规约，但不修改现有综述页、文献数据或索引；无需重建知识库。已有核验页会原样保留，待 Agent 下次更新时才出现待审稿。

## [1.0.44] — 2026-07-23

### 新增

- **自动备份状态现在看得见** —— “设置 → 数据与维护 → 备份与恢复”会显示自动备份是否开启、下次执行时间、当前任务类型和文件进度；后台备份运行时，顶栏也会同步提示。
- **可安全停止正在创建的备份** —— 自动或手动备份可请求停止，PaperPiggy 会在当前文件处理完后清理临时包，不留下不完整 zip；数据恢复为保护完整性仍不能中途停止。

### 修复

- **修复自动备份假占用任务槽** —— 旧版在自动备份尚未到期时会提前占用任务槽却不释放，导致“手动更新知识库”持续误报有任务在运行。现在先判断是否到期，真正开始备份时才占用；若任务线程异常消失，也会自动清理残留占用。
- **备份占用提示更明确** —— 手动更新知识库会区分后台自动备份、手动备份和数据恢复，并告诉用户应等待、停止备份或等待恢复完成。

> **升级方式**：在设置页使用“应用更新”。升级并重启后，旧版遗留的假占用会随进程状态清空；本版不修改文献数据，也不需要重建知识库索引。

## [1.0.43] — 2026-07-23

### 新增

- **顶栏可安全释放检索内存** —— “手动更新知识库”和“设置”之间新增“释放检索内存”。它只释放检索模型与倒排索引占用的临时内存，不删除文献或索引；若界面、对话或 Agent 仍在检索，会明确提示并拒绝释放，不中断正在进行的查询。
- **题录查找输入后自动显示结果** —— 在“文献”页选择“题录查找”后，输入题名、作者、期刊、年份、DOI 或 ISBN 会自动查找，不再需要点击按钮；支持中文输入法，连续输入时只采用最新结果。

### 改进

- **手动更新会说明具体占用任务** —— 若知识库暂时不能更新，现在会区分 Agent 深索、摘要写入、全文深索、备份恢复或其他索引维护任务，不再只显示笼统的“有任务在跑”。
- **全文检索仍由用户明确提交** —— 切换到“全文与相关内容”后，输入研究问题不会自动反复检索，仍需点击“检索全文”或按 Enter。

> **升级方式**：在设置页使用“应用更新”。本版不增加依赖、不修改文献数据，也不需要重建知识库索引。

## [1.0.42] — 2026-07-23

### 改进

- **交付物主题默认保持一屏紧凑** —— Agent 页的“最近的交付物主题”改为“交付物主题”，默认按最近更新时间显示 5 个主题；需要时可展开查看全部，再一键收起。
- **不同窗口宽度下保持易读** —— 正常桌面宽度一排显示 5 张主题卡片，较窄窗口自动调整列数；刷新交付物列表时保留当前展开或收起状态。

> **升级方式**：在设置页使用“应用更新”。本版只调整交付物主题展示，不需要重建知识库索引，已有交付物不会被修改。

## [1.0.41] — 2026-07-23

### 修复

- **应用内指引恢复正常阅读顺序** —— “综述正文可直接编辑”的说明不再作为公告占据新手指引和 Agent 教程最上方，现已分别归入“成果进入综述库”和“综述库（wiki）”对应章节。

### 维护

- 新增发布检查与回归测试：长期指引标题下若再次出现临时更新公告，构建会直接中止；新功能说明必须归入相关步骤或章节。

> **升级方式**：在设置页使用“应用更新”。本版只调整指引内容，不需要重建知识库索引。

## [1.0.40] — 2026-07-23

本版集中加固备份、恢复、应用更新、模型下载和 Windows 进程管理。遇到损坏的压缩包、校验信息不完整、模型与索引不匹配或回滚失败时，应用会停止操作并保留现场，不再带着不确定状态继续执行。

### 安全与可靠性

- **备份与恢复更加稳妥** —— 恢复前检查压缩包路径、链接、完整性和关键文件；先在临时目录验证，再替换现有数据。失败时保留原数据并执行回滚。
- **应用更新和模型下载严格校验** —— 更新包、文件清单和模型归档必须通过大小与 SHA-256 校验；拒绝路径穿越、异常链接和不完整文件，更新失败时不会破坏当前可用版本。
- **Windows 子进程统一安全管理** —— 建库、更新和辅助进程保持无控制台黑窗，并用 Windows Job Object 管理整组进程；不再依赖可能引发 WMI 高负载的 `taskkill /t /f`。
- **危险操作由后端再次确认** —— 删除来源、清理数据等操作必须使用后端生成的预览凭证；维护任务互斥执行，避免两个重任务同时改写同一批数据。
- **模型与索引不匹配时停止检索** —— 当前嵌入后端或模型与既有索引不一致时，会明确提示清空并重建索引，避免返回看似正常但实际错误的结果。

### 界面与说明

- 修复快速切换页面时旧请求覆盖新结果、部分错误原因被隐藏，以及备份名称等文本未安全转义的问题。
- 同步应用内新手指引、Agent 指引、README 和开发文档；MCP 工具数量统一为 39 个。

> **升级方式**：可在设置页使用“应用更新”，也可下载完整安装器覆盖安装。无需重建索引；只有应用明确提示“当前模型与既有索引不一致”时，才需要清空并重建。

## [1.0.39] — 2026-07-22

### 新增

- **综述正文可直接编辑** —— 在综述详情中点“编辑正文”即可修改内容；保存后自动标为已核验，并保留原有标题、来源、主题、链接和 Agent 生成标记。
- **核验状态支持双向切换** —— 已核验的综述可以改回未核验，未核验筛选、列表、搜索结果和详情页会同步显示当前状态。

### 调整

- **移除旧版 Word 导出入口与接口** —— 综述的人工修订统一在应用内完成，不再提供已经废弃的 Word 导出能力。

> **升级方式**：在设置页使用“应用更新”。本版无需重建知识库索引，已有综述和历史版本不受影响。

## [1.0.38] — 2026-07-22

### 使用说明

- **检索摘要设置按生成方式分组** —— “PaperPiggy 自动生成”包含“复用检索引擎的 SiliconFlow Key”和“使用其他 AI 厂商”两种来源；“暂不生成”位于自动生成配置之后。
- **新手指引采用 Agent 主线** —— 基础建库后连接 Agent，通过研究问题、资料范围、交付物和证据标准布置任务；深索、检索摘要、成果交付和知识库维护围绕研究任务展开。
- **在线教程与应用内指引口径一致** —— 教程首页、快速开始、Agent 研究、知识库维护和进阶设置使用当前界面名称与操作路径。

> **升级方式**：在设置页使用“应用更新”。知识库索引无需重建。

## [1.0.37] — 2026-07-22

本版重新梳理首次建库、检索引擎与检索摘要设置，让每一把 Key 的用途、费用和数据流向都能直接看懂。

> **升级方式**：不需要重建索引。已有深索全文、检索摘要和独立摘要模型配置都会保留；旧版已经单独填写过摘要 Key 的用户，会自动继续使用该独立配置。

### 改进

- **PaperPiggy 自动生成改为直接二选一** —— 可以复用检索引擎的 SiliconFlow Key，固定使用当前免费的简单模型；也可以选择 DeepSeek、Kimi、智谱、OpenAI 或兼容服务，自选通常按量收费的模型。旧的“高级摘要配置”已移除。
- **其他 AI 厂商可保存并真实测试** —— 厂商、Key 和模型直接显示在自动生成选项下；测试只调用一次短摘要，并明确提示可能产生少量费用。
- **首次向导只问真正必要的内容** —— 云端检索普通用户只需填写 SiliconFlow Key，已适配的 Base URL、嵌入模型和重排模型不再挤在首启流程；其他兼容检索服务仍保留在设置页高级项中。
- **解释清楚为什么云端检索推荐 SiliconFlow** —— 检索同时依赖嵌入和重排两个专用接口，普通 DeepSeek、Kimi、OpenAI 对话 Key 不能直接替代；这些厂商仍可用于检索摘要或应用内对话。
- **统一深索与检索摘要的说明** —— 深索负责拆分全文、精读和原文定位；检索摘要只是深索后的可选增强，不是题录原摘要，也不替代深索。应用内指引、README、macOS 说明和 GitHub 教程已同步更新。

## [1.0.36] — 2026-07-22

### 修复

- **五格式升级不再误报“需要重建索引”** —— v1.0.35 新增 EPUB、DOCX、Markdown、TXT 时，
  深索健康检查把向后兼容的解析器扩展误判成了旧索引失效。现在会精确识别并自动登记这次兼容迁移，
  既有 PDF 深索、向量和 BM25 均继续有效，不需要清空索引或重新深索；未知的深索规则变化仍会正常提醒。

## [1.0.35] — 2026-07-22

PaperPiggy 的全文链路由 PDF 扩展为五种格式：PDF、EPUB、DOCX、Markdown、TXT。原来的一个导入开关
现在统一表达“只导入有全文附件的文献”，HTML 网页快照仍不作为全文。

> **升级方式**：不需要清空或重建已有索引。旧 PDF 数据会自动兼容；升级后点一次“⟳ 更新知识库”，
> 即可发现 Zotero 或受管文件夹里的 EPUB、DOCX、Markdown、TXT。已有文献、索引、模型、综述和 Agent 文件不会被删除。

### 新增

- **五种全文格式全链路支持** —— Zotero 和文件夹来源、拖入/选择文件、题录抽取、全文提取、切块、
  深索、检索、打开原文、引文定位、MCP 读取与维护状态均支持 PDF、EPUB、DOCX、Markdown、TXT；
  HTML 明确排除。
- **严格的主全文优先级** —— 同一条 Zotero 题录有多个受支持附件时，固定按
  **PDF → EPUB → DOCX → Markdown → TXT** 选择主全文，同时保留附件清单和真实 PDF 兼容字段。
- **不伪造页码的原文定位** —— PDF 继续使用顺序页与期刊印刷页；EPUB 返回章节，DOCX / Markdown
  返回标题段落，TXT 返回行号范围。检索卡、引文核对和 Agent 读取都使用对应定位。

### 调整

- **“只导入有 PDF”升级为“只导入有全文附件”** —— 仍是原来的一个勾选项，升级后保留原勾选状态；
  勾选时，只导入至少带有上述五种附件之一的题录，没有可读全文附件的纯题录不进库。
- **兼容旧索引和接口** —— 内部保留旧设置键、PDF 字段、统计字段与 `/open_pdf` 路由作为兼容层，
  新代码统一使用通用全文字段和 `/open_source`，用户无需清库迁移。

## [1.0.34] — 2026-07-20

本版继续压缩文献、综述和 Agent 页的工具区，并修复历史综述中失效来源键无法通过 Agent 更正的问题。

> **升级方式**：不需要重建索引。v1.0.33 用户可直接使用应用内更新；已有文献、索引、模型、
> 综述和 Agent 文件不会被删除。更新后可让 Agent 再运行一次综述库维护，清除此前被旧接口挡住的失效来源键。

### 改进

- **文献工具栏不再出现横向滚动条** —— “全部文献性质”“全部客观标签”缩为“文献性质”“客观标签”，
  同时压缩筛选器和批量深索按钮；常用控件在宽窗口完整单行显示、没有重叠，窄窗口则安全换行。
- **新手指引入口范围更明确** —— 左上角小猪只在“库总览”页可打开新手指引；离开总览后隐藏书本角标，
  也不再显示可点击反馈。
- **综述工具直接可见** —— 删除“更多工具”，将“体检综述库”和“查看时间线”与刷新、关系图并列显示。
- **Agent 文件夹卡片更紧凑** —— “打开交付物文件夹”“打开资料库文件夹”分别紧跟卡片标题，减少两张卡的高度。

### 修复

- **历史失效来源键可以正常更正** —— Agent 整体重写综述并显式提交来源时，先以新来源替换旧来源，
  再对最终页面执行严格校验；不会再因旧页面里的坏键而拒绝修复。追加模式和普通写入仍保持原有校验强度。

## [1.0.33] — 2026-07-20

PaperPiggy 新增明亮与夜间双主题，并把界面进一步收束为统一的暖白纸张层级：常用操作、卡片、
圆角、阴影和间距采用同一套视觉规则。新手指引与 Agent 使用指引也移到更容易找到、又不挤占
内容的位置；综述的生成与更新统一交给 Agent 完成。

> **升级方式**：本版是界面与交互更新，不需要重建索引。v1.0.32 用户可直接使用应用内更新；
> 也可以运行完整安装器覆盖原位置，已有文献、索引、模型、综述和 Agent 文件不会被删除。

### 新增

- **明亮 / 夜间模式** —— 左上角提供太阳与月亮切换，应用会记住选择；明亮模式保留原来的纸张小猪，
  夜间模式切换为战斗小猪，并采用真正的深色内容层级。
- **小猪头像成为帮助入口** —— 点击左上角小猪即可随时打开新手指引；头像增加常驻书本角标，
  首次使用会显示一次“点小猪，随时打开新手指引”的定位提示。

### 改进

- **统一桌面应用风格** —— 保留深灰窗口框架，内容区采用暖白纸张层级，主操作使用珊瑚粉强调；
  卡片、按钮、圆角、阴影与间距统一，夜间模式和窄窗口同步适配。
- **文献与综述工具区更紧凑** —— 宽窗口尽量保持单行，避免后方留下大块空白；窄窗口改为容器内
  横向滚动，不把整页撑宽，也不隐藏任何筛选或操作。
- **Agent 页信息顺序更清楚** —— 深梅红横幅依次显示“AI 助手驱动你的知识库”、
  “本地库服务在线”和“Agent 使用指引”；指引直接合并进横幅，不再额外占用一整条内容区。
- **综述库更专注于阅读与管理** —— 保留按研究主题分类、检索、筛选、核验和维护工具，去掉冗长说明；
  综述的写作、更新与重新生成统一由 Agent 完成。

### 调整

- 移除网页端“生成综述”“重新生成”入口及对应公开接口，避免网页与 Agent 两套写作流程并存。
- 删除库总览中原有的大幅新手指引卡片，释放首屏空间；完整指引内容仍保留在应用内。

## [1.0.32] — 2026-07-20

综述库进一步收紧为更像桌面应用的工具区：标题与说明保留，但减少纵向占用；筛选、状态和常用操作
分组排列，主题新增按钮与核验状态统一对齐。Windows 任务栏图标也会按当前缩放比例选择正确尺寸，
不再把小图层放大后显示成模糊像素。

> **升级方式**：本版只有界面与窗口图标加载调整，不需要重建索引。v1.0.31 用户可直接使用应用内更新；
> 也可以运行完整安装器覆盖原位置，已有文献、索引、模型、综述和 Agent 文件不会被删除。

### 改进

- **综述库头部更紧凑** —— 缩小标题区留白，搜索、类型、排序和“只看未核验”归为筛选组；刷新、
  关系图保留在外，“体检综述库”和“查看时间线”收进“更多工具”，生成综述入口继续保留。
- **控件节奏更统一** —— 高频按钮统一高度、圆角和文字基线；研究主题的新增按钮改用居中的矢量加号，
  未核验与已核验状态改用颜色圆点，避免全角符号和表情在不同字体下歪斜。
- **窄窗口同步适配** —— 综述工具区在较窄窗口自动分行，筛选与操作仍保持完整，不产生横向溢出。

### 修复

- **任务栏图标更清晰** —— 窗口分别加载符合当前 DPI 的大小图标帧，不再把同一个自动选取的小尺寸
  图标同时交给标题栏和任务栏后放大。

## [1.0.31] — 2026-07-20

应用图标定稿为“深灰圆角底 + 透明外角 + 放大纸张小猪”，并修复 Windows 各处仍可能沿用旧图标的问题。

> **升级方式**：本版不需要重建索引。v1.0.30 用户可直接使用应用内更新；更新后首次启动会同步刷新
> 安装目录、窗口和任务栏图标。也可以运行完整安装器覆盖原位置，已有文献、索引、模型和综述不会被删除。

### 改进

- **小猪图标正式定稿** —— 保留肥小猪与纸张卷角造型，放大主体、强化小尺寸辨识度；深灰圆角底之外
  使用真实透明像素，在浅色和深色桌面上都不再出现白色方角。
- **Windows 多尺寸图标** —— 从同一张 PNG 自动生成 16、20、24、32、40、48、64、128、256 像素
  九档 ICO，桌面快捷方式、开始菜单、安装器、卸载项、窗口和任务栏均使用同一图标真源。

### 修复

- 安装器每次出包都会重新生成 ICO，不再因为构建目录残留而继续打入上一版图标。
- 应用内更新后会同步安装目录根图标并通知 Windows 刷新图标缓存；同时设置稳定的应用任务栏身份，
  避免窗口继续显示 `pythonw` 的默认图标。

## [1.0.30] — 2026-07-19

PaperPiggy 首次引入新的小猪图标方案；应用顶栏与内容框架重新收束，综述库改成按研究主题整理的书架，
页面多时也能按主题、标题和状态快速找到并舒适阅读。

> **升级方式**：本版不需要重建索引。v1.0.29 用户可直接使用应用内更新；
> 旧综述会按其来源与现有 AI 主题的重合度自动归类，不调用大模型、不消耗 API 额度，也不会改写正文。

### 新增

- **综述主题书架** —— 左侧集中显示研究主题和页数，右侧用紧凑目录呈现综述；支持标题搜索、
  类型筛选、未核验筛选，以及按更新时间、标题或来源数量排序。
- **自动归类 + 手动整理** —— 来源相近的综述自动归入现有本地 AI 主题；也可新建自定义主题、
  固定单页、重命名或删除主题。删除主题只会让其中页面恢复自动归类，不会删除任何综述。
- **页内阅读目录** —— 较长综述会根据二、三级标题生成页内目录；正文采用更合适的阅读宽度、
  字号和行距，主题、来源、核验、历史与原文入口仍然完整保留。

### 改进

- **应用框架更像桌面软件** —— 保留稳定的 Windows 原生窗口边框，重做品牌区、导航胶囊、
  激活状态、阴影与内容容器层级；深色主题和窄窗口同步适配。
- **综述列表更安静** —— 删除操作收进每页的“更多”菜单，列表优先展示标题、主题、来源数量、
  日期和核验状态，避免一屏堆满大卡片与危险按钮。
- **新图标接入应用界面** —— 网页品牌位使用新的小猪图标，窗口侧的本地 `.ico` 缓存可随源图更新重建；
  Windows 快捷方式、安装器和多尺寸图标的完整收尾见 1.0.31。

## [1.0.29] — 2026-07-19

综合推荐经过38条真实查询校准，不再让来源档次轻易压过正文相关性；综述库也会在写入前拦住
不存在的文献编号，并找出旧页面中的错误来源和重复标题。

> **升级方式**：本版不需要重建索引。v1.0.28 用户可直接使用应用内更新；
> 更新后可在“综述库 → 体检”查看旧页面的来源错误和重复标题，并交给 Agent 按清单修复。

### 改进

- **综合推荐有真实标尺** —— 建立30条专题查询与8条离题查询的金标集，以25条调参、13条盲测。
  API 检索的来源评价加成由 `0.50` 调整为 `0.30`：调参组前三命中率与前八召回率均为100%；
  盲测组保持两项100%，同时比旧权重取得更高的排序质量。本地检索分数尺度不同，保留原值，不生搬硬套。
- **错一位的来源不再污染综述** —— 新写或修改综合页时，来源 key 必须真实存在；发现错误会整页拒绝、
  列出近似候选，不会只保存一半。对话命中既有综合页时，会展开成其原始论文来源。
- **综述体检新增两类问题** —— 可列出“来源指向不存在的文献”和“重复标题/研究问题”；
  只报告旧问题，不静默改写用户已有内容。
- **摘要名称不再含混** —— Agent 查看单篇文献时，明确分开 Zotero/题录摘要与用于语义检索的 SAC 摘要，
  避免把正常的检索摘要误判成错误。
- **检索摘要质量门槛更完整** —— 新增相邻中文短语重复和异常连续标点检查，明显损坏的生成结果不会进入索引。

### 修复

- 新建综合页时，如果 Agent 把页面标题或同一研究问题也放进正文开头，应用会精确去掉重复外壳；
  正文中正常的小标题不受影响。

## [1.0.28] — 2026-07-19

文献页头部重新排版，解决宽屏下标题、筛选器和批量操作整组换行后留白过大、层级松散的问题。

> **升级方式**：本版只有界面调整，不需要重建索引。v1.0.27 用户可直接使用应用内更新；
> 也可运行完整安装器覆盖原位置，已有文献、索引、模型、综述和自定义评价不会被删除。

### 改进

- 搜索、文献概况/批量操作、筛选/排序分成三层紧凑结构，文献数量与批量按钮保持在同一行。
- 四个筛选框采用稳定宽度并左对齐，宽屏不再无限拉长；中窄窗口自动改为两列或单列。
- 删除常驻的分类操作说明，将对应提示收进“加入分类”按钮，减少无效横条和重复文案。
- 统一搜索框、筛选框和操作按钮的高度、间距与底色，宽屏和窄屏均无横向溢出。

## [1.0.27] — 2026-07-19

“浏览”和“检索”合并为同一个“📚 文献”页面：既能按题名、作者等题录信息直接找论文，
也能切换到全文与相关内容检索，不再需要在两个页面之间来回跳转。

> **升级方式**：本版没有新增运行组件，也不要求重建索引。v1.0.26 用户可直接使用应用内更新；
> 也可运行完整安装器覆盖原位置，已有文献、索引、模型、综述和自定义评价不会被删除。

### 改进

- **文献页统一查找与检索** —— 顶栏删除独立“检索”，原“浏览”改名为“文献”；同一个搜索框可在
  “题录查找”和“全文与相关内容”之间切换。题录查找完全本地运行，支持题名、作者、期刊、年份、
  DOI、ISBN 等字段；全文模式保留相关片段、页码、上下文、打开原文和找相似能力。
- **客观标签成为正式筛选项** —— 文献页新增动态客观标签下拉框，显示当前范围内各标签数量，
  可与手动/AI/Zotero 分类、真实文献性质、深索状态和排序叠加。
- **默认浏览更可预期** —— 文献列表默认按最新入库排序；“综合推荐”保留为主动选择，去掉容易
  造成主观质量暗示的“值得先读”和“建议深读”文案。
- **分类栏更安静** —— 手动分类默认展开，AI 分类与 Zotero 分类默认收起；用户手动调整后继续记忆状态。
- **文库概况更平衡** —— 最近入库固定显示三篇，评价分布在左侧半栏内上下居中，两侧视觉密度一致。

### 清理

- 删除普通文献页的“导出选中引文”按钮及无调用的前端格式化代码；Agent 写作和核验依赖的底层
  引注引擎继续保留。

## [1.0.26] — 2026-07-19

库总览重新编排为更紧凑的文库概况：评价分布与最近入库合并展示，文献构成不再堆叠说明和箭头；
评价规则会直接列出用户改过的项目，并可逐项或一次性恢复出厂设置。

> **升级方式**：本版没有新增运行组件，也不要求清空或重建索引。v1.0.25 用户可直接使用应用内更新；
> 若更新后看到“题录分类规则需要更新”，只需运行一次“手动更新知识库”，无需重新深索或重建语义索引。

### 改进

- **文库概况更集中** —— 四档评价与最近入库进入同一张概况卡片，不再用“全库四档评价”等重复标题；
  学科与更新提示也收进卡片标题区，减少总览的纵向占用。
- **文献构成更易浏览** —— 操作说明与标题同排，客观标签移除装饰性箭头；默认先展示主要项目，
  其余项目按需展开，点击标签仍可直接查看对应文献。
- **自定义规则看得见、退得回** —— 折叠的“评价规则”显示实际改动数；展开后置顶列出每项的
  当前值与出厂默认值，可恢复单项或恢复全部。把某项改回默认值后不再留下冗余自定义记录。
- **规则修改立即生效** —— 目录/文献性质映射调整会即时更新评价分布、浏览和检索排序，
  不触碰客观目录数据，也不要求重建索引。
- **升级提示更准确** —— 只有题录分类指纹变化时明确提示运行一次手动更新，并说明无需深索或重建；
  正常的运行环境、深索和语义索引状态不再挤进警告区域。

## [1.0.25] — 2026-07-19

库总览恢复简洁：开发维护信息不再进入用户应用，评价规则默认收起，客观标签可直接点进对应文献；
单篇改档菜单也会明确显示当前评价和自动规则。

> **升级方式**：本版没有新增运行组件，也不要求重建索引。v1.0.24 用户可直接使用应用内更新；
> 也可运行完整安装器覆盖原位置，文献索引、模型、综述库、手动改单篇档位和 Agent 文件都会保留。

### 改进

- **开发者增强预设按私人配置固化** —— “法学（开发者增强）”采用用户确认的 14 项出厂映射；
  “夯到拉”继续共用同一份规则，只替换显示名称。普通“法学”仍保留更通用的评价梯度。
- **总览不再堆满设置** —— 文献性质与客观标签合并为带页签的“文献构成”，评价规则移入默认收起的
  高级面板；四档分布不再展示容易误解的内部权重。
- **客观标签可以查看明细** —— 点击 CSSCI、SSCI Q1、三大刊、书籍等标签会进入浏览页并应用真实
  后端筛选，可继续叠加文献性质、深索状态、收藏范围、排序和分页。
- **单篇改档状态更清楚** —— 菜单标出当前档位、自动/手动状态和选中项；手动改档后会说明恢复自动
  将回到哪个档位，客观标签本身仍不被改写。

### 清理

- 删除误放进用户产品的“目录来源与半年检查”总览卡片和“文献目录半年检查”Agent 任务。
  升级时只删除这个精确命名的旧开发任务目录，不影响用户的其他定时任务或交付物。

## [1.0.24] — 2026-07-19

PaperPiggy 现在用同一套评价覆盖期刊论文、书籍、书章、学位论文、法源、案例、标准、报告、
数据集、预印本、会议论文和网页，不再让非期刊材料落进一套不适用的期刊六档。

> **升级方式**：本版没有新增运行组件，也不要求清空或重建索引。v1.0.23 用户可直接使用应用内更新；
> 也可运行完整安装器覆盖原位置，文献索引、模型、综述库、手动改档和 Agent 文件都会保留。

### 新增

- **全类型文献统一评价** —— 普通页面只显示一个客观标签，如“三大刊”“CLSCI”“SSCI Q1”
  “TSSCI”“书籍”“博士论文”或“法源”；主观评价统一为权威、顶级、核心、普通四档。
- **真实文献性质筛选** —— 浏览页和 MCP 都可按十二类真实性质过滤，并与收藏、主题、深索状态、
  排序和分页叠加；网页白皮书、报纸中的正式司法意见会按内容性质归类。
- **目录与性质映射可调整** —— 库总览集中展示四档比例、内部权重、客观标签/性质明细、目录来源、
  上游版本和半年检查状态；用户可只调整当前学科的映射，不改写客观目录数据。
- **正式 TSSCI 与 JCR 目录** —— 正式 TSSCI 法律学门与个人台湾偏好表分离；SSCI/JCR 目录更新为
  ShowJCR JCR2025 快照，并记录来源提交和检查日期。
- **娱乐显示名** —— 新增“法学（开发者增强：夯到拉）”，完全复用开发者增强规则、权重、排序、
  缓存和手动改档，只把四档显示为“夯 / 顶级 / 人上人 / NPC”，并明确标注仅供娱乐。

### 改进

- **书籍和书章全部按权威、学位论文全部按顶级**；法源、案例、标准按顶级，报告按核心，
  权威机构数据按核心，其他数据与网页等按普通。旧索引缺少新增字段时会安全降级，下次增量建库自然补齐。
- **综述来源更透明** —— 每条来源显示唯一客观标签和低干扰四档，顶部汇总来源构成；
  “未核验”缩到标题或卡片右侧状态区，不再占据正文视觉中心。
- **写作工作流先追库内参考文献链** —— “写论文与综述”“跨学科发散与补文献”都会先从核心文献
  反向追踪参考文献，保留来源页码/序号，优先补外文和台湾材料，并单列“文献使用与缺口”。
- **半年目录检查任务** —— Agent 资料库会生成可迁移的检查任务说明；应用只登记和展示，
  未接入所用 Agent 的调度器时不会伪称任务已自动运行。

### 兼容性

- 旧 T1/T1b/T2/T3/T4/T5 手动改档继续读取并折叠到四档；新改档使用稳定的四档代码。
- 手动改档只改变评价，客观标签保持不变；已有文献、索引和用户数据无需迁移或重建。

## [1.0.23] — 2026-07-18

应用更新后的提示现在只保留真正需要处理的事项，不会因用户选择暂不下载本地模型而长期占据 Agent 页面。

> **升级方式**：本版没有新增运行组件，v1.0.22 用户可直接使用应用内更新；也可运行完整安装器覆盖原位置，文献索引、模型、综述库和 Agent 文件都会保留。

### 改进

- **本地模型不再被误当成更新待办** —— 不下载本地模型是可选的使用方式，不再显示“缺少本地模型”提醒，也不会妨碍其他待办完成后提示自动消失。
- **更新提示可暂时收起** —— 有待办时可手动收起提示框，继续使用其他功能；重新检查时会根据实际状态再次显示。
- **提示只列出仍待处理的内容** —— 已完成或已忽略的用户内容不会留在列表中；索引规则与运行环境都已处理后，提示框会自动隐藏。

## [1.0.22] — 2026-07-17

“维护”现在是一套完整闭环，而不是等用户逐个指出异常：Agent 会先全量审查工作流、索引、深索、
检索摘要和综述库，自动处理安全事项，只把真正需要选择的事情交给用户，最后复核并提交全面总结。

> **升级方式**：本版没有新增运行组件，v1.0.21 用户可直接使用应用内更新。仍在较早版本，
> 或应用提示“需完整安装器”的用户，请运行 v1.0.22 完整安装器覆盖原位置；不用卸载，文献索引、
> 模型、综述库和 Agent 文件都会保留。

### 改进
- **所有工作流升级为强制入口** —— PaperPiggy 会在 Agent 工作区根部生成 `AGENTS.md` 与
  `CLAUDE.md`。任务命中内置工作流时必须先读取；每份工作流都明确了触发条件、开工检查、用户决策点、
  完成标准和最终报告，不能看到待办后只解释原因。
- **“维护”默认执行全量审查** —— 新增统一维护体检，覆盖模板升级、索引与深索、PDF / OCR 阻塞、
  检索摘要、Wiki 更新建议和 Wiki 结构检查，并把结果分成“可自动处理、需用户决策、外部阻塞”。
- **选择“交给 Agent 生成”后由 Agent 真正完成摘要修复** —— Agent 读取正文、提交摘要，应用统一做
  质量检查、重嵌入和结果复核；不会偷偷改走用户配置的服务端 API，也不会消耗该 API 的额度。
- **Wiki 建议不再只看一小批** —— 待审建议支持分页和逐条留痕处理；新主题只作为候选，Agent 必须
  核对后标记为已更新、已新建、不需要或受阻，维护结束时能确认是否真正清零。

### 安全性
- **Agent 可安全合并用户定制与新版模板** —— 合并前校验文件未被并发修改，写入前自动留下
  `user-backup`，既保留用户规则，也能消除长期悬挂的“待合并”提示。
- **摘要修复采用整批质量闸门和失败恢复** —— 任一摘要不合格时整批不写；重嵌入失败时恢复原摘要，
  不把半完成状态伪装成成功。

## [1.0.21] — 2026-07-17

应用更新后，用户自己改过的工作流和综述库规约不再只是静默留下一份 `.new.md`：现在会在
Agent 页明确提示，并提供差异查看与安全合并入口。

> **升级方式**：本版没有新增运行组件，v1.0.20 用户可直接使用应用内更新。仍在较早版本，
> 或应用提示“需完整安装器”的用户，请运行 v1.0.21 完整安装器覆盖原位置；不用卸载，文献索引、
> 模型、综述库和 `0_Agent资料库` 都会保留。

### 改进
- **用户改过的工作流不再悄悄错过新版** —— 未修改的出厂工作流仍会自动升级；用户修改过的文件
  保持原样，新版另存旁本，并在 Agent 页显示“待合并”。可查看逐行差异、复制一份合并任务给
  Agent，或在自动留下 `user-backup` 后直接采用新版。
- **Agent 会主动知道哪些规则待合并** —— MCP 接入指令会列出待处理的工作流与 Wiki 规约，要求
  Agent 保留用户个性化规则、合并新版新增要求，遇到冲突先询问，不能擅自覆盖。
- **增加统一升级健康检查** —— 同时检查 Agent 模板、综述库规约、索引生成规则、Python 运行环境
  和本地模型版本；应用代码更新了、但配套内容仍停在旧版时，会明确说明应采取的动作。

### 安全性
- **增量更新增加运行环境指纹** —— 未来新版若需要不同的 Python 运行组件，应用内更新会在替换
  程序前安全停止，并引导使用完整安装器，避免“代码更新了、运行环境没更新”的半升级状态。
- **发布护栏覆盖全部出厂模板** —— 12 份 Agent 模板的当前版本 hash、Wiki schema 与正文版本
  都会在打包前自动核对，漏登记将直接中止构建。

## [1.0.20] — 2026-07-17

浏览页现在能直接看清每类文献有多少篇；设置页重新分组后，也更容易找到检索、建库、备份和更新功能。

> **升级方式**：本版没有新增运行组件。已通过 v1.0.17 或更新版本的完整安装器补齐 OCR 组件的用户，
> 可以直接使用应用内更新；仍在更早版本，或应用提示“需完整安装器”的用户，请运行 v1.0.20
> 完整安装器覆盖原位置，不用卸载，用户数据会保留。

### 改进
- **筛选项直接显示篇数** —— “已深索 / 未深索、OCR / 原生文字层、摘要有效 / 异常 / 缺失”都会
  显示当前左侧范围内的文献数量；切换分类、收藏夹或时间范围时，数量同步变化。
- **摘要状态更容易辨认** —— 使用有颜色且含义稳定的“🧬 摘要有效、🟠 摘要异常、⚪ 摘要缺失”，
  便于快速定位需要处理的文献。
- **设置页按任务分成五类** —— 检索、建库、外观、数据与维护、应用分别放置，原有设置和数据不变；
  两套应用内教程及相关跳转路径已同步更新。
- **Agent 交付物卡片更整齐** —— 日期与文件统计固定分两行显示，不再因内容多少造成卡片下方大片空白。

### 修复
- 老版本已经深索、但没有记录“正文来源”的文献，会按历史事实归入“原生文字层”，不再造成
  原生文字层筛选只显示极少数新文献。

## [1.0.19] — 2026-07-17

深索结果与检索摘要现在更容易核对：浏览页可直接筛出 OCR 文献和摘要异常；Agent 写论文、
做综述时则会先覆盖更多文献，再对选定材料深入取证。

> **升级方式**：本版没有新增运行组件。已通过 v1.0.17 或更新版本的完整安装器补齐 OCR 组件的用户，
> 可以直接使用应用内更新；仍在更早版本，或应用提示“需完整安装器”的用户，请运行 v1.0.19
> 完整安装器覆盖原位置，不用卸载，用户数据会保留。

### 改进
- **浏览页可直接筛 OCR 与检索摘要状态** —— 原有筛选新增“本地 OCR / 原生文字层”和
  “有有效摘要 / 摘要异常 / 摘要缺失”，可直接定位扫描件和需要修复或补生成摘要的文献。
- **检索摘要增加严格质量闸门** —— 乱码、连续重复、失控长文和“没有正文”的过期占位摘要不再计为完成，
  也不再用于新的嵌入；界面和 MCP 会分别显示有效、异常、缺失数量及异常原因。只有用户主动点
  “修复 / 补生成摘要”时才会重写并重嵌入。
- **Agent 检索改为先广后深** —— 发现型检索同一篇最多返回 2 段，候选不足时不再用同一篇的重复段凑数；
  `verify_claim(keys=[...])` 等定向核验仍可在已经选定的文献内多段取证。内置“写论文与综述”和
  “跨学科发散与补文献”工作流已同步这一用法。

### 修复
- 深索队列已清空时，MCP 不再误报“运行中”；PDF 缺失、损坏、OCR 等提取异常会单列显示。
- `read_source` 只读指定页时不再把该页误报成全文末页；论断核验不再返回空引文或重复证据。

## [1.0.18] — 2026-07-17

升级安装不再把 Codex、Claude 等客户端持有的 PaperPiggy MCP 笼统显示成一串“Python”，
也不会在后台连接仍存活时尝试强行覆盖程序文件。

> **升级方式**：已通过 v1.0.17 完整安装器补齐运行组件的用户，无需为本修复重新覆盖安装，
> 可以继续使用应用内更新。仍在 v1.0.16 或更早版本，或者虽已增量更新到 v1.0.17 / v1.0.18
> 但仍提示“需完整安装器”的用户，请直接运行 v1.0.18 完整安装器覆盖原位置；不用卸载，
> 用户数据会保留。本版的 MCP 占用预检修复只存在于完整安装器，增量包不会改变安装器行为。

### 修复
- **升级时明确指出是谁占用了程序文件** —— 完整安装器会在写文件前识别 PaperPiggy 主程序，
  以及由 Codex、Claude、Visual Studio Code、Cursor 等客户端启动的 MCP 后台连接，并显示对应 PID。
- **不再自动结束 Agent 任务** —— 检测到占用时，安装器只提示用户保存工作并退出相关客户端，
  预检只提供“重试 / 取消”，不提供“忽略继续”，也不会代替用户结束进程。
- **防止 MCP 刚关掉又被自动拉起** —— 正式写入前再复查一次，避免客户端在第一次检查后重新
  启动 MCP，导致安装中途失败。

### 内部
- 占用检测通过 Windows WMI 只读查询进程树，同时覆盖 `python.exe` 与 `pythonw.exe`；
  完整命令行只用于内部判定，不写入日志或用户界面。全新安装没有旧运行环境时直接放行。

## [1.0.17] — 2026-07-17

扫描件现在可在深索时自动做本地 OCR；Agent 交付物与定时任务的展示和打开也更准确。

> **本版升级方式**：新增了本地 OCR / OpenCV 运行组件，已有用户请下载完整安装器，直接覆盖安装到
> 原位置即可，**不用卸载**；索引、综述、设置和 Agent 文件都会保留。若先用了应用内增量更新，
> 新版会继续提示你下载并运行完整安装器。

### 新增
- **扫描件自动本地 OCR** —— 深索 PDF 时优先读取原有文字层，只对没有文字的页面调用
  本地 OCR。识别结果只写入 PaperPiggy 索引，不修改原 PDF，也不上传文献。
- **交付物主题可直接打开** —— Agent 页的最近交付物卡片可直接跳到对应文件夹，同时正确
  统计子文件夹里的成果。

### 改进
- **区分扫描件、附件丢失和 PDF 损坏** —— 不再把所有“读不到正文”都笼统显示为“需 OCR”；
  界面会告诉用户哪些可自动 OCR，哪些需要补齐附件或更换 PDF。
- **定时任务展示更诚实** —— 成果位于子文件夹时不再误报“0 个文件”；任务目录缺少
  `任务.md` 时会明确提示未识别原因。
- **弹窗风格统一** —— 备份、恢复和应用升级不再使用会显示 `127.0.0.1:8770` 的浏览器原生弹窗。

### 内部
- OCR 新增了 RapidOCR / OpenCV 等运行时依赖；首个包含该功能的版本需通过完整安装器升级，
  旧版的“应用内增量更新”不会补齐 Python 依赖。
- 发布构建会按锁文件与实装依赖生成运行时指纹；依赖变化时强制刷新 `bundle/python` 并真实导入
  RapidOCR / OpenCV 等核心组件，避免安装器误带上一版的旧 Python 环境。

## [1.0.16] — 2026-07-17

启动和闲置时大幅少占内存：检索组件改为真正按需加载，并在空闲后自动释放。

### 改进
- **检索组件改为用到时才加载、空闲后自动释放** —— 打开应用只读取轻量的库目录；第一次在界面、对话或
  Agent 中检索时，才加载检索模型和 BM25。最后一次检索完成后默认空闲 10 分钟释放，正在进行的检索绝不会
  被中途打断。
- **释放时间可在「设置 → 检索内存」调整** —— 可选 5/10/20/30/60/120 分钟或“不自动释放”，并会显示
  当前是已释放、正在加载、正在检索，还是还剩多久释放。释放只腾出临时内存，不会删除文献、索引或设置；
  代价是释放后的第一次检索要重新准备，本地模型通常比 API 模式等待更久。

### 修复
- **不再在启动时把全部正文和 1024 维向量转成 Python 对象** —— 向量继续留在 LanceDB，检索时只按需读取
  融合后的少量候选，而且候选详情不带向量列。20 万段规模下可避免约 10 GB 的常驻内存和长时间冷启动；
  检索排序逻辑不变。

### 内部
- 建库、增量导入、AI 主题聚类等维护路径也不再无意读取整张向量列；需要遍历向量时改为紧凑的分批处理。

## [1.0.15] — 2026-07-16

真正修好「重开应用先白屏、加载页没起效」。

### 修复
- **「重开应用先白屏、加载页只闪一下」** —— 加载页的关键样式直接内联，外部样式文件尚未返回时也能
  立即正确铺满整屏；同时先把界面发出去，再开始读取知识库，让加载过程不再是一段莫名白屏。

### 内部
- 外部 AI 助手（Claude Code 等）经 MCP 连接本库时会启动的 `mcp_server` 进程，现在会在其客户端进程
  退出后自动结束，不再残留孤儿进程堆积（只影响用 MCP 的人；纯用应用的用户不涉及）。

## [1.0.14] — 2026-07-16

后台 / 外部改动自动刷新（不用点也不用重开）+ 修「关掉再打开白屏」+ 关窗彻底关闭后端。

### 改进
- **库、综述、交付物在后台或被外部 AI 助手改动后，你正看着的那一页会自动刷新** —— 不用点刷新、更不用
  重开。外部助手写回一篇综述、或有新文献入库，当前页会在几秒内自己更新（分域感知：只刷真变了的那一域
  对应的页，不会把你正在看的别的列表也刷得跳一下）。
- **修「关掉应用再打开是白屏、要过好一会才显示」** —— 根因是首次打开要把整个知识库读进内存（大库约 1~2
  分钟），而窗口在数据就绪前就打开了、界面又没有加载提示。现在打开时会显示一个明确的「正在加载知识库…」
  进度页（带计时），加载完自动进入，不再是莫名白屏。

### 修复
- **关闭应用后，后台服务会被彻底关闭**（含它启动的子进程），不再残留 python 进程越积越多。以前「关掉后
  后端没关掉」——连上已有服务、或有子进程时清理不干净——会留下后台进程堆积。

## [1.0.13] — 2026-07-16

给「数据在使用中变了、却要重开应用才看得到」的地方补上刷新：库总览、综述库、浏览页各加「🔄 刷新」，Agent 页的深索进度接进后台轮询自动更新。

### 改进
- **库总览、综述库、浏览页各加了「🔄 刷新」按钮** —— 后台深索 / 生成摘要在跑，或外部 AI 助手改了库
  （新建综述、增量入库等），**点一下刷新就能看到最新，不用再重开应用**。
  - 库总览：刷新深索 / 检索摘要进度、最近入库等。
  - 综述库：刷新综述列表 + 建议横幅（AI 助手写回综述后立刻可见）。
  - 浏览页：重新扫描文献列表、收藏夹树计数、知识库分类、总数。
- **Agent 页的「深索 / 检索摘要」进度接进了后台轮询** —— 深索或补摘要在别处推进时，这张卡会像库总览
  一样自动走字，不用手动刷新、更不用重启（此前它只在打开 Agent 页时加载一次就锁死）。

### 内部
- 全面梳理前端各页的刷新机制：确认设置页（模态弹窗每次打开都重拉）、顶栏状态 / 进度条（挂在 4 秒实时
  轮询上）、检索 / 对话页的范围下拉（切页即重拉）本就不会滞后，无需改动，只给真正「加载一次就锁」的
  几处补了刷新。

## [1.0.12] — 2026-07-15

一条数据安全修复（Zotero 库临时读不到时不再误清空索引）+ Agent 页两处体验改进（刷新按钮、交付物列表美化）。

### 修复
- **Zotero 数据目录临时读不到时，不再静默清空整个索引**（重要，关乎数据安全）。
  以前：你在设置里指定了 Zotero 库的位置（比如放在 D 盘），若某次「更新知识库」时那个盘 / 目录
  恰好读不到（盘没插好、目录被改名），程序会**悄悄回退**到一个早已废弃的空 Zotero 库，把它当成
  「你的库现在几乎没文献了」，进而把已建好的整库索引（含深索成果）**当作"已删除"清空** —— 全程不报错。
  现在两道防护：① 你**显式指定过**的库若当前读不到，直接**停下报错**（"读不到你配置的 Zotero 库…"），
  绝不改用别的库；② 即使别的原因让读到的文献数骤减，清理前也会**先过一道阈值**（读到空库、或一次要删
  过半 → 拒绝清理并提示），宁可暂留几条残留，也不误抹整库。

### 改进
- **Agent 页「⏰ 定时任务」和「📦 最近的交付物主题」各加了「🔄 刷新」按钮** —— 新建的定时任务、
  新写好的交付物，**点一下刷新就能看到，不用再重启应用**（此前这两块只在应用启动时加载一次）。
- **「最近的交付物主题」列表改成卡片式**：每个主题一张卡（名称 + 日期 · 文件数 · 含说明），长名字
  自动换行、不再被截断，深浅色都适配；空的时候也会显示（附刷新提示），不再"有了交付物却看不到"。
- **法学检索分词词典补充了一批术语**（如「刑事附带民事公益诉讼」「性侵害未成年人」「量刑折扣」等），
  相关主题检索时更容易整词命中。

## [1.0.11] — 2026-07-15

修「深索显示永远卡在 99%」——其实早就深索完了，只是那几篇扫描件被算成了"待深索"。

### 修复
- **首页「① 深索」卡（以及深索面板、Agent 页、深索邀请）把扫描件也算进"待深索"，导致永远显示
  「99% · 还有 N 篇」**，让人误以为卡住或没完成。实际上那 N 篇是**扫描件/无正文**（没有文字层、
  需 OCR，本来就无法深索）。现在进度按"**已处理**"算（已深索 + 已确认扫描件）：全部有正文的都深索完、
  剩下的都是扫描件时，显示 **100% · 已全部深索完成 ✓**，并注明"另有 N 篇扫描件无法深索"，
  不再有误导性的"还有 N 篇 / 深索全部未深索文献"。真正还有可深索文献时，照旧正常提示。
  （1.0.9 修过顶栏进度条与深索详情面板的同类问题，这次补齐首页与其余入口。）

顶栏自动提示新版本；并为「朋友在 Mac 上从源码跑」做了跨平台适配（Windows 行为不变）。

### 新增
- **右上角自动更新提示**：有新版本时，顶栏右上角（⚙ 设置 左边）会出现「🎁 有新版 vX →」，
  点一下直接进设置的更新区一键升级。启动时会静默检查一次（设置 → 应用更新 里有「启动时自动检查
  新版本」开关，**默认开**，不想让它每次启动连 GitHub 就关掉）。右键徽标可忽略当前版本，
  发更新的版本时会自动重新提醒。
- **macOS 从源码运行支持**（面向个人自用，非公开发布的打包）：数据落点改到
  `~/Library/Application Support/PaperPiggy`、依赖按平台分流（macOS 装 pyobjc、跳过 Windows 专属的
  pythonnet）、Zotero 目录探测支持 macOS profile、打开文件夹/取消建库等改为跨平台。
  详见 [MAC-从源码运行.md](MAC-从源码运行.md)。**以上改动在 Windows 上一律无变化。**

## [1.0.9] — 2026-07-15

为「重装一次库、深索重来一遍」铺路：只导 PDF 更严格、深索不再假装卡在 99%、并新增一键清空重建。

### 修复
- **深索到 99% 就「卡住不动」（其实没卡）**：所有有正文的文献都嵌完之后，还有最后一步——
  重建 bm25 检索索引（几十万个文本块，分词+建索引要好几分钟）。这期间进度条冻在 99%、
  深索计数也不动，看着像死机，其实在收尾。现在这一步会明确显示「**深索已完成，正在重建检索索引（收尾中）**」，
  不再误导你以为卡死。
- **「只导入有 PDF 的文献」以前会放行法源类**：法规/案例/标准/报告即使没有 PDF 也照样进库，
  和开关字面意思不符、也让一堆无正文的纯题录混进来。现在**严格只导有 PDF 的**——
  重要法条你自己配上 PDF 再入库即可。

### 新增
- **一键「清空并从头重建索引」**（设置 → 🧹 清空并从头重建索引）：想把索引推倒重来时用。
  它**只清索引**（全文提取、切块、向量、bm25、深索标记、整库状态），
  **明确保留**综述库、收藏夹分类、期刊分级、检索摘要（花过 API 钱的）、页码映射、设置、
  `0_Agent` 工作区。清空后点「⟳ 更新知识库」重建题录+语义（会**自动去重、按“只导 PDF”重新导入**），
  再「深索全部」重新深索。旧的提取/切块会移到一个备份目录、可手动捞回。
  ⚠️ 向量索引会被丢弃、需重新嵌入，操作前建议先在「💾 备份与恢复」打一份含索引的备份。

## [1.0.8] — 2026-07-15

一批体验与正确性修复：浏览标签刷新、深索面板、导入去重、升级不再闪黑窗、充值提示。

### 修复
- **深索过程中浏览页仍显示旧的「扫描件」标签**：文献早已重新提取入库、后端也已实时更正，
  但浏览列表加载一次后整段深索期间不刷新。现在深索/自愈有进展时，浏览徽标会**自动跟上**
  （不动你的勾选、滚动、分页）。
- **深索详情面板「整库深索中 / 已暂停 / 暂无正在深索」三态混显**：暂停按钮、「正在深索」列表
  是**队列**的概念，整库深索时不适用却还显示。现在整库深索时只留干净的进度 + 「停止整库深索」。
- **应用内升级时会闪一下终端黑窗**：更新器的一个子进程漏了「无窗」标志。已修，并把全项目
  所有子进程统一无窗（以后新功能也默认无窗）。

### 新增
- **导入去重**：Zotero 里同一篇被重复添加（不同条目）会各占一行，现在入库时自动合并。
  ⚠️ **绝不删已深索/已入库的副本**——去重只清没有任何深索产物的纯题录重复，你花 API 钱嵌好的
  一份永远保留。（对已全部处理过的老库不会动手，主要防将来新导入的重复。）
- **充值提示**：设置 → 检索引擎处提醒——SiliconFlow 账户余额为 0 时连免费模型也会报 403，
  建议先充 ¥1，之后免费模型即可一直正常用。

## [1.0.7] — 2026-07-15

自愈 1.0.6 之前那次崩溃留下的「僵尸扫描件标记」——1.0.6 修好了 PDF 提取，但之前被误判的
文献仍卡着进不了深索、浏览还显示「扫描件」。本版让它们自动补回。

### 修复
- **修好提取后，文献仍显示「🚫 扫描件·需 OCR」、深索进不去**：崩溃期大批 PDF 假失败时被
  打了 `no_text`（扫描件）标记，这个标记是**粘性**的、从不复查——即便后来 PDF 已重新提取、
  切块成功，深索仍永远跳过它们、浏览仍显示扫描件。现在深索前会**对账自愈**：凡标了扫描件、
  但其实已切出真实正文块的，一律清除标记、补入深索。真正无正文的扫描件仍正确保留标记。
  （升级后点一次「深索全部」即可把这些文献补回；真正的扫描件不受影响。）

## [1.0.6] — 2026-07-15

修复深索时应用崩溃 + 大量文献被误判为「扫描件」——根因是 PDF 提取库的线程不安全。

### 修复
- **深索时整个应用窗口崩退**：PDF 提取用 4 线程并发调 PDFium，而 **PDFium 不是线程安全的**，
  多线程同时访问会损坏其内部堆（Windows 事件日志确诊：故障模块 `pdfium.dll`，
  异常码 `0xc0000374` 堆损坏 / `0x80000003`）。改为**所有 PDF 提取全局串行**，绝不并发。
  实测：同一批 500 个 PDF，并发时约 70% 失败/偶发崩溃，串行后 100% 成功、不崩。
- **大量有文字的文献被误标「🚫 扫描件·需 OCR」**：正是上面并发竞态导致的随机
  「Failed to load」（同一 PDF 单独打开明明好好的），被误判成扫描件、深索篇数虚低。
  串行修复后，重跑「深索全部」会自动重试这些篇并成功入库。
- PDF 提取加瞬时失败重试（文件被 OneDrive / 杀软 / Zotero 短暂占用时多试两次）。

> 升级后请重新点一次「深索全部」：之前因崩溃/误判没入库的文献会自动补上（不会重复劳动，已成功的跳过）。

## [1.0.5] — 2026-07-15

修复 API 模式下批量深索/建索引的崩溃、假完成与卡死。

### 修复
- **API 模式深索全库会崩**：嵌入调用没有兜底，一次 API 失败（余额不足→403、限流、网络超时）
  就会带 traceback 掀翻整个后台任务。现在：致命错（密钥/余额/模型名）立即优雅中止并给人话提示；
  限流/网络类跳过该篇、连续多次才判停；**任一篇没嵌成功都不会谎报完成**，已嵌的部分照常保存、可续跑。
- **崩了却提示「✓ 深索完成」**：失败后紧接着的后台任务会把全局退出码覆写掉，前端据此误报成功。
  现在后端失败时不再立即接排新任务、前端只认「退出码 0」的阳性证据才报完成，否则一律「⚠ 深索中断」。
- **进度条卡死在「正在提升检索质量 X/Y」**：语义嵌入中途失败后，进度条会永远停在那儿谎报进行中。
  现在区分「真在跑」（动进度条）与「上次未完成」（静态提示「点『手动更新知识库』继续」）。

## [1.0.4] — 2026-07-15

修复应用内更新的一个致命 bug，并增强国内下载可靠性。

### 修复
- **应用内更新点「下载并升级」报 `name 'threading' is not defined`**：启动器缺 `import threading`，
  升级的最后一步（拉起更新进程 + 关窗）直接崩，导致 1.0.2 / 1.0.3 的应用内更新**根本用不了**。
  已修。⚠️ 因为这个 bug 就在旧版里，**1.0.2 / 1.0.3 用户须用安装器手动装一次 1.0.4**；此后即可应用内一键升级。

### 新增 / 变更
- **下载重试**：检查更新、下载增量包都改为失败自动重试几次——国内连 GitHub 偶发超时，多试往往就通。
- **国内镜像兜底**（⚙ 设置 → ⬆️ 应用更新 → 国内镜像）：填一个镜像目录前缀（Gitee Release / 对象存储），
  应用内更新会先试 GitHub、连不上就走镜像。只需镜像那个约 1MB 的增量包。设置方法见 README。

## [1.0.3] — 2026-07-15

调整三项出厂默认（只影响**新安装**；已装用户的现有设置保持不变，可在设置里随时改）。

### 变更
- **默认「只导入有 PDF 的文献」**：Zotero 里没有 PDF 的纯题录不进库，库更干净、卡片更完整。
  ⚠️ 代价：没有 PDF 的法条/法规/网页法源会被挡在库外——需要收全法源的，在
  「⚙ 设置 → 只导入有 PDF」里关掉即可。
- **期刊分级学科默认「法学（开发者增强）」**：含台湾刊 / 顶尖外文法评 / 外文权威社科刊，
  外刊不打折。标准法学分级仍可在设置里切回。
- **深索摘要默认「交给 Agent 生成」**：省 API 额度、契合以 Agent 为主的用法。
  注意：应用内自己点「深索」时不会自动补摘要，需让 Agent 跑深索时顺带生成、或在
  浏览页/库总览手动补。想让应用自动生成，在设置里切到「服务端生成」并填 Key。

## [1.0.2] — 2026-07-15

新增应用内一键升级。[下载](https://github.com/DrinkTea905/paper-piggy/releases/tag/v1.0.2)

### 新增
- **应用内一键升级**（设置 → ⬆️ 应用更新）：检查、下载、升级到新版本，一步到位，
  升级完自动重启。**只替换程序本体**，你的索引、综述、Agent 交付物、期刊分级等数据
  **一律不动**——升级前先在暂存区验证新版能启动、再做替换，失败自动回滚到旧版；
  你改过的 `.py` 代码会保留到「你改过的旧代码-<版本>」文件夹，不会丢。
  （从本版起，以后升级不必再去 GitHub 下安装器；1.0.1 升到本版仍需手动下一次安装器。）

## [1.0.1] — 2026-07-15

修复首个版本的两个启动问题，并重定发布形态。[下载](https://github.com/DrinkTea905/paper-piggy/releases/tag/v1.0.1)

### 修复
- **桌面快捷方式点了没反应**：启动器 `PaperPiggy.vbs` 用 `SW_HIDE` 启动子进程，
  被 pywebview 继承成「隐藏窗口」——应用其实每次都启动成功，只是窗口看不见，
  用户每点一次就多一个后台幽灵进程。快捷方式改为直接指向 `pythonw.exe`，绕开 VBS。
- **重复启动**：应用没有单实例保护，双击 N 次会起 N 个进程抢同一端口。
  现在再次启动会把已有窗口拉到前台、自己退出。
- **Agent 模板旁本 `.new.md` 被静默覆盖**：提示用户「对照合并」，用户在旁本里写的
  合并笔记会被下次启动的出厂原文盖掉。现在旁本被改过就另存为 `.new.2.md`，绝不覆盖。

### 变更
- **只发 Inno 安装器，不再出便携 zip**。数据与程序同目录后，便携版「删旧文件夹、
  解压新版」的常规升级姿势会把索引、综述、写好的论文一次性删光；安装器升级不会
  （只覆盖程序本体）。
- **改为用户级安装**（`%LOCALAPPDATA%\Programs\PaperPiggy`，可在向导里改到任意盘），
  安装不再需要管理员权限、不弹 UAC。
- **数据与程序放同一个文件夹**：索引、模型、综述、Agent 交付物全在安装目录内，
  整个文件夹拷走即可换机。安装目录不可写时自动回退到 `%LOCALAPPDATA%\PaperPiggy`。
- 用户数据目录由 `%LOCALAPPDATA%\LocalKB` 改名为 `PaperPiggy`。
- **更新方式说明**：目前没有应用内自动更新；升级 = 到 GitHub 下新版安装器覆盖安装，
  只覆盖程序本体（`app\`、`python\`），索引、综述、写好的论文等数据不受影响。
  （注意与设置里的「自动更新知识库」区分——那是知识库增量索引，不是版本升级。）

### 新增
- **备份与恢复**（设置 → 💾 备份与恢复）：把综述、Agent 写的论文与记忆、收藏夹、
  期刊分级等「丢了就再也没有的东西」打成一个 zip。备份位置可指到 OneDrive／坚果云
  的文件夹实现云备份（同步静态 zip 是安全的，而云盘直接同步向量索引会把它搞坏）。
  可选连向量索引一起打包（换机免重建）；可设自动备份。API 密钥默认不进备份包。

---

## [1.0.0] — 2026-07-14

首个公开版本。**已撤下**——存在启动窗口不显示的 bug（见 1.0.1 修复），release 与其安装包已删除，请用 1.0.1。

### 新增
- 项目指引本地化：`CLAUDE.md`（AI agent 总纲）、`docs/ARCHITECTURE.md`、`docs/MAINTENANCE.md`
- 开源三件套：`LICENSE`（Apache-2.0）、`THIRD-PARTY-NOTICES.md`、`CHANGELOG.md`
- git 版本控制（此前靠 15 个手工备份目录，已回放为 git 历史）

### 变更
- **PDF 提取引擎改用 `pypdfium2`**（原 `pymupdf4llm`）。原方案会间接引入 `pymupdf_layout`
  （Polyform Noncommercial：禁止商业使用、非 OSI 开源）与 AGPL 的 PyMuPDF，
  与本项目的 Apache-2.0 冲突，**导致此前根本无法合法开源发布**。
  实测替换后：中文提取字符数逐篇完全相同（无内容损失），文本质量更好
  （原方案会把中文标点错序），单篇提取快 20~100 倍。
- `requirements.txt` 的 `transformers` / `tokenizers` 版本上限修正为与实装一致
  （此前写 `<5` / `<0.22`，而实装是 5.13.0 / 0.22.2，照着重装会得到一个从未验证过的组合）。

### 修复
- **Word 导出静默降级**：`python-docx` 声明在 requirements 却从未装进分发包，
  导致综述导出恒降级为 `.md` 且不报错。
- **干净机上本地模式必崩**：分发包的 Python 运行时缺 `msvcp140.dll` / `msvcp140_1.dll`
  （`onnxruntime` 的硬依赖），在未装 VC++ 2015-2022 的机器上 import 即 `WinError 1114`。
- MCP 工具表漂移：文档写「28 个工具」，代码里实际 32 个（`gen_mcp_doc.py --check`
  本可检出，但从未被调用）。
- `.claude/launch.json` 与 MCP 配置指向已不存在的目录。

### 移除
- 分发包目录 `LocalKB/`（2.1G 测试数据与构建产物）。构建改为从源码 + `build/` 资产生成。

### 打包
- Inno Setup 安装器（177 MB）+ 便携 zip（282 MB）+ updater 用的增量包
- 模型（约 900 MB）走 GitHub Release 按需下载，支持断点续传与 sha256 校验；API 模式无需下载
- 缺 WebView2 时安装器静默安装 Evergreen Runtime

---

## 历史

2026 年 6~7 月的开发轮次（UX 改造、期刊分级、gist 对标、Agent 层、wiki 综合层等）
未按版本号发布，其设计文档存档于 [`_archive/`](_archive/)，代码演进见 `git log`。
