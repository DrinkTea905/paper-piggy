> # ⛔ 废稿 · 请勿执行 · 永不发布
>
> **用户 2026-08-14 判定整个 v5 重训作废。禁止安装、禁止执行、禁止作为改造基础。**
>
> 判废时点：阶段 0—4 已完成、阶段 5（前向验证）尚未开跑，我刚交出本文件与两份 A/B 提示词。
> **判废理由：用户未说明**（截至本戳记时）。见 `v5失败复盘.md`——那里只记已确证的事实，
> 不替用户编造理由。
>
> 阶段 0—4 的台账（病症清单、语料冻结清单、134 段范例库、规则与反例、阈值校准）是
> **实测记录**，本身未被判废，可作材料查阅；但不得据以恢复本文件。

---

# 前向验证 · 乙稿提示词（对照组：只给规则，不给句子）

> **跑的顺序：先甲稿，后乙稿。** 乙稿要复用甲稿产出的《引证核验表》当证据底座——
> 取证是取证，写法是写法，把它们分开，A/B 的唯一变量才是**给不给句子**。
>
> 用法：**再新开一个对话**（不要用跑甲稿的那个），复制下面分隔线内的全部内容。

---

任务：把我桌面这份文件润色并补完大纲与主要内容，写成一篇约 24,000 字的论文初稿 ——
`C:\Users\Lsj13\Desktop\低龄未成年人核准追诉程序的定位检视与制度重构 - 副本 - 副本.docx`

## 〇、先说清楚这一稿是干什么的

这是一次**对照实验的对照组**。同一个题目、同一份大纲、同一份证据底座，会有两稿：
另一稿写作前读了 134 段三大刊真刊原文，**这一稿不读**，只给规则。

**所以：本稿全程不得打开范例库**
（`D:\Onedrive\AI\知识库应用\docs\设计\少年司法初稿v5\范例库\`，以及任何 `key.md` 形式的精读文件）。
读了就毁掉对照。除此之外的一切标准一条不减。

## 一、工作流

1. 先 `read_workflow("重新起草（用户大纲优先）")`，声明采用——它是覆盖层：我已给出详细大纲，
   **结构指标一律降为参考、第一闸门（骨架卡）整体跳过**，且这是推倒重写（零继承、工序倒置四轮）。
2. 再读这份文件：
   `D:\Onedrive\AI\知识库应用\docs\设计\少年司法初稿v5\论文初稿（少年司法·v5）.md`
   **但只照办以下各节**：§0 三条原则、§1 工序、§3 十四条规则、§4 材料与证据底座、
   §4b 用户决策点、§5 体检、§7 硬失败、§8 交付、附录 A／B／C。
   **§2「范例库：先读后写」整节跳过，不要执行。**
3. `read_workflow("论文初稿（少年司法版）")` 不要读（v3，已被取代）。若因故读到，其《成文技艺手册》
   整册不作为依据：选型决策树、骨架卡、句型库、标题词组池、成稿自检表一概不用。

## 二、这份文件的地位

它是我自己写的，已经经过取舍，是本题唯一的依据。

- **不要动大的框架**：六章及其章序、各章分节，照它来。
- 但**章名、节名的用语该改就改**——我原来的标题写得口语化，一律改成学术标题该有的名词短语，
  并把改动列成对照表给我。
- 文件里我用【】（）标出的**待办占位**，**逐项落实，一项都不要漏**。

## 三、证据底座：复用甲稿的，不要重新取证

甲稿已经跑完第一轮取证，产出了完整的《引证核验表》，在
`0_Agent交付物/核准追诉新稿D_v5甲/引证核验表.md`。

**本稿直接用它当证据底座**：每条法条、每位学者的每个立场、每个页码、每处数据都已核验并标注出处。
你可以（也应该）用 `read_source` 复核其中任何一条，但**不要另起一轮全面检索**——
两稿的证据必须相同，差异才归得到「给不给句子」上。

⚠️ **只读那一份《引证核验表》，不要读甲稿目录里的其他文件**（正文、README、台账、检索发现日志一概不读）。
读了甲稿正文，这一稿就成了甲稿的改写，对照实验作废。

## 四、零继承：本题的具体清单（违反即整轮作废）

1. 记忆**只调** `read_project_memory(scope="core")`。不得调 `scope="topic:核准追诉"`、`scope="all"`。
2. **wiki 综合页一页都不许开**；`search_localkb` 一律传 `include_wiki=false`。
3. **这些目录不许打开、不许读、不许 grep**：`核准追诉定位检视与制度重构/`、`核准追诉平行稿/`、
   `核准前程序重写/`、`核准追诉的两种逻辑/`、`先议程序的刑事化/`、`核准追诉新稿A_无手册/`、
   `核准追诉新稿B_用手册/`、`核准追诉新稿C_v4/`，以及**甲稿目录里除《引证核验表》外的一切**。
4. 检索起点只从我这份文件与那份《引证核验表》长出来。

**判断标准**：你写下的每一句实体判断，都要能指出是从我这份文件、还是从《引证核验表》的哪一条、
还是从你这一轮亲自复核过的哪一页原文来的。指不出来就不要写。

## 五、体检与验收

- 交稿前跑两步体检：① 7 项禁区表；② 表外扫描。**两步缺一不可。**
- 体检只是体检，不是验收。
- 盲读由我另行安排（两稿一起进盲读，判读者不知道哪份是哪份）。台账要留全。

## 六、交付

- 落 `0_Agent交付物/核准追诉新稿D_v5乙/`，附 README、台账。
  **不要写进甲稿目录，也不要写进任何旧稿目录。**
- 成稿为可打开的 `.docx`，正文约 24,000 字；**另存一份 `_正文.md` 纯文本中间稿**供盲读与体检使用。

## 七、其他

- 分工按默认派子代理规则办。派出去的子代理一并被告知第三、四节的禁止，
  尤其「不许读范例库」「不许读甲稿正文」。
- **视野要开阔**：我国台湾地区、外文文献、美国制度、国际条约与软法都要用上；姚建龙的文章要用上。
- 三条格式规则交稿前逐条 grep 报数字：「前引／同前注／同上注／Ibid」**0 处**；
  例外五类之外的中文数字 **0 处**；正文具名指称他人研究 **0 处**。
