# 前向验证 · 甲稿提示词（用范例库）

> 用法：**新开一个对话**，把下面分隔线内的全部内容复制过去。
> 为什么要新开对话：本对话的主代理已经读过丙稿全文与全部诊断，判断已被污染；
> 新对话没读过，前向验证才有意义。

---

任务：把我桌面这份文件润色并补完大纲与主要内容，写成一篇约 24,000 字的论文初稿 ——
`C:\Users\Lsj13\Desktop\低龄未成年人核准追诉程序的定位检视与制度重构 - 副本 - 副本.docx`

## 一、工作流

1. 先 `read_workflow("重新起草（用户大纲优先）")`，声明采用——它是覆盖层：我已给出详细大纲，
   **结构指标一律降为参考、第一闸门（骨架卡）整体跳过**，且这是推倒重写（零继承、工序倒置四轮）。
2. 再读这份文件，**全文照办**：
   `D:\Onedrive\AI\知识库应用\docs\设计\少年司法初稿v5\论文初稿（少年司法·v5）.md`
   它是新训练出来的 v5 工作流草案，本稿是它的第一次实跑。冲突处以「重新起草」的覆盖层为准。
3. **`read_workflow("论文初稿（少年司法版）")` 不要读，它是 v3，已被 v5 取代。**
   若因故读到，其《成文技艺手册》整册**不作为本稿依据**：§1 选型决策树、§2 骨架卡、§3 句型库、
   §4 标题词组池、§7 成稿自检表一概不用。v5 里一条都没有这些东西——它给的不是模板，
   是真刊原文与判断型规则。

## 二、★ 范例库：先读后写（这是 v5 的核心，不许跳过）

范例库在 `D:\Onedrive\AI\知识库应用\docs\设计\少年司法初稿v5\范例库\`，
12 个 .md 文件，装着 **134 段三大刊真刊原文**（已逐段回原文校验，零编造），按 9 个论证动作分类。

**规矩：写某一章之前，先读完对应动作标签下的全部范例，再动笔。**

| 你要写的 | 先读完这些标签 |
|---|---|
| 批判章／交锋段（第二、三、四章） | **下判断** ＋ **真交锋** |
| 事实与数据段 | **实景** ＋ **数据推论** |
| 学说梳理（第四章） | **命名** ＋ 真交锋 |
| 比较法段落 | **比较法** |
| 对策章（第五章） | **立法建议** |
| 任何实质章 | **段落推进** ＋ **让步** |

三条纪律：① **读，不是抄**——与他人任一原句共享 ≥8 连续汉字即硬失败，比对基准包含范例库全文；
② **不模仿罕见值**——范例里出现一次的写法就是出现一次，不要当必做动作；
③ **读完就写，不要边写边翻**。

## 三、这份文件的地位

它是我自己写的，已经经过取舍，是本题唯一的依据。

- **不要动大的框架**：六章及其章序、各章分节，照它来。
- 但**章名、节名的用语该改就改**——我原来的标题写得口语化（「为什么其他学说也不行」「特别程序说为什么行」
  「没有规定的：准用……」），一律改成学术标题该有的名词短语，并把改动列成对照表给我。
- 文件里我用【】（）标出的**待办占位**（要算的比例、要列的原文、要找的文献、要设计的条文、要理顺的层次），
  **逐项落实，一项都不要漏**。

## 四、零继承：本题的具体清单（违反即整轮作废）

1. 记忆**只调** `read_project_memory(scope="core")`。不得调 `scope="topic:核准追诉"`，不得调 `scope="all"`。
   本题的既往结论都在那份主题档案里，默认不进上下文——别自己打开。core 里是长期规则与工具经验，放心用。
2. **wiki 综合页一页都不许开**。不得调用 `get_wiki_page`、`list_wiki`、`build_digest`、`research_outline`、
   `get_backlinks`、`pending_wiki_updates` 等任何综合层读取工具；`search_localkb` 一律传 `include_wiki=false`。
   检索到的文献原文可以用，wiki 页不行。
3. **这些交付物目录不许打开、不许读、不许 grep**：
   `核准追诉定位检视与制度重构/`、`核准追诉平行稿/`、`核准前程序重写/`、`核准追诉的两种逻辑/`、
   `先议程序的刑事化/`，以及 `核准追诉新稿A_无手册/`、`核准追诉新稿B_用手册/`、`核准追诉新稿C_v4/`。
   **最后三个是同题旧稿，读了这一轮就白跑。**
4. 检索起点**只从我这份文件长出来**：文件里出现的每一处引证，加上文件里标出的待找文献，就是全部种子。
   不预设「应该还有某某文献」。

**判断标准**：你写下的每一句实体判断，都要能指出是从我这份文件、还是从你这一轮亲自读过的哪一页原文来的。
指不出来就不要写。

## 五、工序里本题特有的两件事

1. 第一轮《引证核验表》要**覆盖我文件里的每一处引证**——每条法条、每位学者的每个立场、每个页码、每处数据。
   库内没有的法条直接联网取官方原文并双来源交叉验证。
2. 同时**单独跑一路检索**：第三章那处待找文献——**刑事程序本身（而非刑罚）对未成年人造成伤害的
   我国台湾地区与英文文献**。这是全文最缺的一块材料。

**第一轮之前不要给我「这里应该改成什么」的实体意见。**

## 六、体检与验收（v5 §5、§6，一步都不能省）

- 交稿前跑两步体检：① 7 项禁区表（6 项零阈值禁令＋1 项上限）；② 表外扫描
  （`工具/outlier_ngram.py`，找出「本稿超过全部真刊」的新表达）。**两步缺一不可。**
- 体检**只是体检，不是验收**。全绿不代表写得好——上一版就是五项全绿而我判定很烂。
- 盲读由我另行安排，你不必跑；但《检索发现日志》与台账要留全（见 v5 §8：每章的 R11 答案、
  R7 动词清单、R3 两句最强命题）。

## 七、交付

- 落 `0_Agent交付物/核准追诉新稿D_v5甲/`，附 README、《检索发现日志》、台账。
  **⚠️ 不要写进 `核准追诉新稿A_无手册/`**——那是旧稿目录，覆盖了就没法回源了。
- 成稿为可打开的 `.docx`，正文约 24,000 字。
- **另存一份 `_正文.md` 纯文本中间稿**（不含脚注定义行），供后续盲读与体检使用。

## 八、其他

- 分工按默认派子代理规则办，不必问我。派出去的每个子代理都要一并被告知第四节那四条禁止，
  尤其「wiki 一页都不许开」和「那几个目录不许碰」。
- **视野要开阔**：我国台湾地区、外文文献、美国制度、国际条约与软法都要用上；姚建龙的文章要用上。
- 三条格式规则交稿前逐条 grep 报数字：「前引／同前注／同上注／Ibid」**0 处**；
  例外五类之外的中文数字 **0 处**；正文具名指称他人研究 **0 处**。
- 全文写完后，**把《引证核验表》单独留一份完整的**——下一轮对照组要用同一份证据底座，
  不要让它散在各处。
