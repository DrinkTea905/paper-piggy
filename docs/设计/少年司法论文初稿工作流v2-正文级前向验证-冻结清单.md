# 少年司法论文初稿工作流 v2：正文级前向验证冻结清单

> 冻结时间：2026-07-29 08:52:05 +08:00  
> 开发分支：`codex/juvenile-justice-workflows`  
> 基线提交：`98f614e`  
> 正式知识库：`D:\PaperPiggy`，本轮只读。

## 一、候选运行时文本

| 对象 | exact SHA-256 |
|---|---|
| `agent_ws._WF_JJ_DRAFT` | `e0549417f34d64042525ccf581e669008f5b2041e4ad1553b0a16288d8c867ce` |
| `agent_ws._JJ_DRAFT_CRAFT_HANDBOOK` | `64006a99fb61e8e068e3f1bb372307edee5cb1989c2c5b06f20e4b3215297328` |
| `少年司法论文初稿工作流v2-24篇规则冻结与运行时映射.md` | `288f0760342cb00ef7450b8d95d8a1ab091f8f3e09891b3f587f112fa975e612` |
| `少年司法论文初稿工作流v2-正文级前向验证任务集与判定表.md` | `3d6f033ba98fd1d9375dceca800fe60349b100be2e7f697647fb6ce0255f8e58` |

散列均以 UTF-8 字节计算；运行时主工作流和手册直接对 Python 常量正文计算，避免把源码中其他改动混入。

## 二、盲测封存

六题生成与原始输出冻结之前，禁止生成代理读取：

- B01 `SVNSFFZ3`
- B02 `TXY75SMN`
- B03 `XDVR96NW`
- B04 `QIDFLN9E`
- B05 `CMF3HLNP`
- B06 `HIDEIB2X`
- B07 `E9ZYIT5Z`
- B08 `N3BY3BRD`

如检索命中，只记录“盲测排除”，不得打开正文。B01—B08 仅在六题候选文本和原始输出冻结后用于独立盲测。

## 三、生成隔离

1. D01、D02、T03、T04、M05、S06 的生成者只得到对应题面、候选主工作流和伴随手册，不得到评分表、其他题输出或盲测正文。
2. 生成者可只读检索正式知识库并逐页核验非盲测原始来源；不得写入正式安装。
3. 原始输出落盘后只读。评审发现的错误写入独立评分、回源或回归文件。
4. 后续若修改候选运行时文本，必须产生新散列和新一轮验证，不能沿用本冻结清单的结果。

## 四、2026-07-30 DOCX 交付增量

用户在原正文级验证完成后明确要求：最终论文成稿必须是可正常打开并经检查的 `.docx`，Markdown 只能作为中间稿、写作设计或核验记录。此次变更没有回写或冒充原六题的冻结输入，原散列和原始输出继续保留。

增量后的运行时散列为：

| 对象 | exact SHA-256 |
|---|---|
| `agent_ws._WF_JJ_DRAFT` | `b45c5c9a50eec7b989ed2ee6f05b9958b63b0076473146978dc863ba1c19b281` |
| `agent_ws._JJ_DRAFT_CRAFT_HANDBOOK` | `2887b5141c1f3a1a6ad5470f32d49fd984e8f63073737fa6ae63fc5466bc1bb5` |

本次只新增交付格式、落点、Word/Zotero 域保护和打开或渲染检查要求，不改变双路线、两个人工闸门、结构原型、论证规则、来源核验或六题题面。增量验证由模板迁移、自动附带、用户修改保护、DOCX 文案断言和全量回归测试承担；原六题评分仍只归属于第一节的冻结运行时。

## 五、2026-08-03 v3 全面替换（本清单第三节第 4 条所要求的新散列与新一轮验证）

**v2 的运行时文本已被 v3 整体替换。** 本节按第三节第 4 条留档；第一至四节的散列、六题原始输出与评分**继续保留为 v2 的历史记录，不因本次替换而失效，也不得被援引为 v3 的验证依据**。

替换后的运行时散列：

| 对象 | exact SHA-256 |
|---|---|
| `agent_ws._WF_JJ_DRAFT` | `8d8e7061d598c95c5dae5827284b6d45e00d570502137572eb746ab6f7fc521e` |
| `agent_ws._JJ_DRAFT_CRAFT_HANDBOOK` | `428a1f83ce309548c0305abb888364c895afe5def7b10b0212048d015062ef37` |

**替换原因**：用户对 v2 两份真实初稿的结论是「没有效果」。事后以成品为证据的诊断（见 `docs/设计/少年司法初稿v3/00-失败诊断（以成品为证据）.md`）查明：v2 的 23 条冻结规则中可生成文本的仅 3 条，且无一条关于章节结构；唯一可执行的数字是一组恒真统计区间，反而诱发了 2/2/2/2/2 与 5/5/5/5/5 的配额化章节。

**v3 的主要变更**：① 废除「教义学／理论—制度建构」二分法（28 篇实测显示其对章数零预测力、对章功能负预测力），改为由 36 篇陈瑞华论文归纳的七类结构原型与五问选型决策树；② 结构规则由审计表改为可照搭的骨架卡，全部阈值来自 36 篇实测；③ 事实命题与规范命题分开管，规范命题不因缺少实证材料而降低论断强度；④ 新增书面语层与工作流术语泄漏防护。

**v3 的验证方式（与 v2 不同，不采用代理自评分）**：
1. 全量精读 36 篇陈瑞华论文（2001—2026，593 个可读印刷页），逐篇 15 项实测；
2. 用新判据回测 v2 的 A、B 两稿——旧量表下 8 次运行 8 次通过（87—93 分），新判据下 A 稿 18 项过 6 项、B 稿过 5 项；
3. 按 v3 走完两道人工闸门产出第三稿并交付 DOCX，由主代理独立机器验收（不采信代理自评）：口语词 20→0、强判断标记 1→20、段末回收句 2→24、标题密度 2155 字/标题、章际回指 26 处；
4. 用户对第三稿的形态判断为通过，并据此授权发布。

**仍未做的**：v2 的六题前向验证未在 v3 上重跑；另两篇第三稿（监督考察协议、矫治教育性质）仍为 v3 修订前的文风，未按最终规则重写。

## 六、2026-08-10 局部修订：选型准入自检、自建骨架（卡⑧）与子代理分工

**本节是当前运行时。** 第五节的散列自本次修订起转为历史记录；v3 的结构阈值、两个人工闸门、证据要求与硬失败清单**全部保留不变**，本次只改「骨架从哪来」和「谁来干活」。

修订后的运行时散列：

| 对象 | exact SHA-256 |
|---|---|
| `agent_ws._WF_JJ_DRAFT` | `7bf354c39b692f58d8c933f22a5e20e8c16abcf386febb17455703edbe04b199` |
| `agent_ws._JJ_DRAFT_CRAFT_HANDBOOK` | `28f1bb30f95ac4af7d11bf0958ae9021de58e3e3782a64e3aa33be0e6814e903` |

> 两个散列在同日经多次修订（见本节末「补充修订」与「自审订正」）。

**修订原因（用户 2026-08-10 反馈）**：「两张骨架卡太死板，有些题目不能硬套进去的也套。」
机器复核确认这不是执行偏差，而是规则本身封死了出口：
1. §1 决策树没有真出口 —— Q3 写不出 → Q4，Q4 否 → Q5，而 Q5a 的「不能」又**退回类⑤**。任何题目最终都会落到某一叶子，于是工作流第一闸门里写着的「走不到叶子就自建骨架」**永远触发不了**；
2. 第一闸门原第 3 条要求两卡来自**两个不同叶子**且**章数不得相同**，在第 1 条已把题目锁进某一型之后，第二张卡只能硬凑；
3. 手册 §2 末尾的「诚实条款」（`LD68HVXY` 归不进任何一张卡）写成了脚注式提醒，没有卡的地位，也没有可执行的栏目。

**本次变更**：
① 决策树每一问补齐出口，末端指向新的 **类⑧ 自建骨架**；Q1 追加「无成批条文可引不得硬走类①」、Q4a 追加「写不出三条反驳则不用类④」、Q5 追加「列不出 ≥4 处空缺不用类⑤」；
② 手册 §2 把「归不进去的一篇」升格为 **卡⑧ 自建骨架**，给出立骨四步（先守底盘八条 → 章序由材料依赖定 → 篇幅按材料厚度分 → 末章形态自定）、允许偏离底盘八条的登记方式，并明写**自建不豁免证据底座、第二闸门、起草各条、硬失败与 18 项完成标准**；
③ 工作流第一闸门新增**准入自检**（四条出局判据，出局理由须写成具体一句话），两卡来源放开为原型＋原型／原型＋自建／自建＋自建三种平权组合；「章数不得相同」改为可写明材料锁定理由的有条件约束；
④ 硬失败新增 2-b（判出局仍交卡、自建偏离底盘八条未登记）；最终报告新增准入自检结果与两卡来源。

**并行修订**：四条研究工作流均新增「分工：是否派子代理（开工前问一次，由用户决定）」小节 —— 逐环节给默认建议（精读取证／两张卡隔离起草／独立校核建议派，正文分节起草默认不派，理由是章际回指等**跨章约束**并行必破），且**用户回答前不得派**；子代理交回的是材料不是成稿，两个闸门与硬失败自检不得转包。

**验证方式**：`check_guides.py` 九项全过（含 ④b 出厂模板指纹登记）；`src/tests` 全量 323 项通过，其中新增 `test_juvenile_draft_allows_self_built_skeleton_on_equal_footing`（断言决策树旧死循环出口已消失、卡⑧ 存在、自建不豁免）与 `test_research_workflows_ask_user_before_spawning_subagents`。

**补充修订（同日，用户第二轮反馈）**：

用户提出两点，均属第一闸门的结构性缺陷，已一并改入同一版：

1. **选型发生在读文献之前** —— 旧流程「开工前检查 → 第一闸门（选型出卡） → 证据底座（完整精读）」把完整精读放在选型之后，等于让代理**凭题目猜结构**。已在第一闸门前新增「选型前侦查」节：候选池题录 20—40 条，从中挑 6—10 篇核心：完整精读 2—3 篇（挑材料最新的与最被引／最对立的）、其余只读标题树＋引言结论，另核现行法版本时点（不计入篇数）；只回答七问（通行名称／争议派别具名／条文与试点是否成批／有无对手学说／空缺是否 ≥4 处／哪块材料最新最密即最长章／有没有人已经写过），产出一页《选型前侦查表》与两张卡一起交用户。**硬上限 10 篇**，到顶仍定不下选型即停下来把卡住的那一问交给用户，不许靠继续读拖延。
   **边界写死**：侦查阶段的局部阅读**不产生可引用论断**，只能用来选结构；正文引用一律走证据底座的完整精读并核到印刷页。准入自检的出局理由必须引侦查表条目，不得凭印象。
2. **骨架卡只有骨头没有肉** —— 卡上原有逐字标题树却没有"这篇准备怎么写"。新增第 ③ 栏「每章、每节准备写什么」：每个一级单元 2—3 句、有节的每节再补一句，逐条交代**本章回答哪个问题｜用哪类材料回答（规范／理论／事实，注明手上有没有、出处在哪）｜准备得出什么判断**；判据是**遮住标题后仍能认出是哪一章**，"介绍……的基本情况""对……进行分析"视为该栏未交；有节的还须写出每节区别于同章其他节的那一点，写不出即合并。
   原 ③—⑧ 顺延为 ④—⑨，自建卡附加栏顺延为 ⑩⑪；两卡差异要求由「①②③⑥」改为「①②③④⑦」，并明写"两卡各章准备写的内容雷同等于只有一条路线"；硬失败新增 2-c（未侦查即出卡／七问无支撑／③ 栏空话）。

同一轮并行修订《论文初稿（通用暂用版）》：新增第 0 步「先做小规模摸底再谈路线」（同样的篇数刻度与"局部阅读不得落成引注"边界），第 5 步的三级提纲要求逐部分附同格式的内容简述。

**第三项修订：四条研究工作流强制写《检索发现日志》**（用户 2026-08-10 拍板，起因是权重校准调研）。
调研查明：应用**不落盘任何检索历史**（`server.py` 的 `/search` 与 `/chat` 均不写盘；全 src 的 `C.DATA / "xxx"` 落点里无任何 query/history 文件；前端"最近检索"只有 15 条 localStorage 且 pywebview 隐私模式关窗即毁）。因此旧待办里「等用户攒够 20+ 条真实查询」这条路**结构上永远不会自达成**。
现要求每次检索留一行，落 `0_Agent交付物/<主题>/检索发现日志.md`，表头固定为 `| 轮次 | 查询词（逐字） | 名次 | key | 题名 | 处置 | 理由 |`，处置只用 `采用`／`弃用`／`待核`，另起一节记漏召回。**列名与顺序不得改动**——这是检索校准的唯一数据源，格式一变历史日志就聚合不起来。四条工作流的完成标准／最终报告均已加入该项检查。

**自审订正（同日，用户要求复查本轮改动后自查发现）**：
1. 骨架卡栏号由 ①—⑧ 顺延为 ①—⑨ 时，**手册卡⑧ 里有两处引用漏改**——「排完写进第 ⑦ 栏当作本卡自己的不可调换处」实为第 ⑧ 栏、「在第 ⑩ 栏写明偏离哪条」实为第 ⑪ 栏。两处都会把执行者指到错误的栏位（⑦ 是末章形态、⑩ 是"像哪两型"），已订正。
   > 教训：编号重排属于"跨文件的机械改动"，工作流正文改了而随附手册没跟上；两份文本互相引用栏号时，重排后必须全文 grep 一遍旧编号。
2. 通用暂用版第 0 步的摸底刻度**算不上数**：原写"完整精读 2—3 篇、只读标题树＋引言结论 4—6 篇……读到 10 篇仍定不下路线就停"，而 2+4=6、3+6=9，**永远到不了 10 篇**，停止线形同虚设。已改为"挑 6—10 篇核心：完整精读 2—3 篇，其余只读标题树＋引言结论；读满仍定不下就停"。少年司法版正文本就写的是"其余"（自洽），但**八处面向用户/agent 的文档把"其余"写死成了 4—6 篇**，同样订正。

**仍未做的**：本次修订**未跑真实题目的正文级前向验证** —— 自建骨架路径尚无实测成稿，其"底盘八条 + 材料依赖定序"是否真能产出与七类原型同等质量的结构，只有推理依据（底盘八条来自 36 篇全样本，比单一原型更普遍），没有成品证据。侦查阶段的「6—10 篇 / 完整精读 2—3 篇」这组刻度同样**未经实测**，是按"够回答七问"推出来的，第一次实跑后应据实调整并回填本节。

## 七、2026-08-13 局部修订：记忆分区与「按主题取档案」

**本节是当前运行时。** 第六节的散列自本次修订起转为历史记录；v3 的结构阈值、两个人工闸门、
证据要求、硬失败清单与 18 项完成标准**全部保留不变**，本次只改工作流第 1 步「开工前读什么」。

修订后的运行时散列：

| 对象 | exact SHA-256 |
|---|---|
| `agent_ws._WF_JJ_DRAFT` | `03cbe7a672d4dfa28a91a38bfd0ac2d07489bf2f9668728d5f8c134da90fb1f1` |
| `agent_ws._JJ_DRAFT_CRAFT_HANDBOOK` | `28f1bb30f95ac4af7d11bf0958ae9021de58e3e3782a64e3aa33be0e6814e903` |

> 手册未改，散列与第六节相同。

**修订原因（用户 2026-08-13 需求）**：项目记忆此前是单文件、一路 append，涨到 669 行 / 77938 字符，
超过 MCP 客户端单次工具返回上限被自动转存为临时文件，Agent 要分四次才读得完；而闸门规定开工必读，
这笔开销每次开工都要付。**但真正的问题不是 token**：该文件里混着长期偏好、工具经验、各主题结论与
历史流水账四类东西，当用户要求「推倒重写一篇论文、以我的新文件为准、旧结论作废」时，第三类会随闸门
强行进入上下文，Agent 无法不看见——实测导致把既往结论当成了本轮判断依据。靠提示词约束
「看见了不要用」不可靠，需要**物理上读不到**。

**本次变更（仅第 1 步）**：
① 开工读的是记忆 **core 视图**（项目记忆.md + 工具经验.md 全文 + 主题档案清单），
   主题档案正文**默认不读**，要用才 `read_project_memory(scope="topic:<主题名>")` 单独取；
② 明写**推倒重来的题目不取旧主题档案**——取了就无法不看见；
③ 收尾结论写回 `append_project_memory(target="topic:<主题名>")`，**一个研究主题只有一个档案**
   （产品侧新建时查重，撞了退回既有那份）；
④ 子代理告知项由「先读项目记忆」改为「先读项目记忆 core」，与主流程同口径。

**未触及**：选型前侦查的篇数刻度、准入自检、两卡要求、证据底座、硬失败清单、完成标准。
本次**不构成**对 v3 结构规则的任何放松。

**验证方式**：`check_guides.py` 十二项全过（含 ④b 出厂模板指纹登记、① 工具表 42 个工具）；
`src/tests` 全量通过。

**仍未做的**：与第六节相同——自建骨架路径与侦查刻度仍未经真实题目的正文级前向验证。
本次修订同样**未跑真实题目**：「按主题取档案 / 推倒重来不取」在真实写作里是否真能挡住旧结论渗入，
目前只有机制保证（core 视图不含主题正文，是结构性的），没有成稿证据。

---

## 八、2026-08-20 · v6 运行时文本上线（整体替换 v3/v4 血统）

**变更**：`_WF_JJ_DRAFT` 由带《成文技艺手册》的 v3/v4 血统版本，整体替换为 2026-08-15 重训、
连跑三稿（E/F/G）验收的 **v6**，并于 2026-08-20 依「用户亲手成稿 vs 同题 AI 稿」的对照补入三条禁区。
此前 v6 只存在于开发仓库 `docs/设计/少年司法初稿v6/`，从未进入出厂模板，故用户机器上
`read_workflow` 一直读到旧版。

**当前散列**（本节即测试 `expected_current` 的同步落点）：
- `_WF_JJ_DRAFT`：`202f217199ffb5055494755ba6f8941ea7ce6e0935015b34d14b9afe60caaca9`
- `_JJ_DRAFT_CRAFT_HANDBOOK`（已停用，见下）：`b172c4b0fbc729b04e3a7615b27094d27ee8d5955969c678179ce30db0198012`
- `_JJ_DRAFT_STRUCTURE_REF`（新增伴随材料）：`c0b69d392e4cf5f65b81619a70456a66f7ca1e56f79cafe0acbce518a3dd4c7c`

**随 v6 一并废止的 v3/v4 机制**：选型决策树、七类结构原型与准入自检、选型前侦查的篇数刻度、
两张骨架卡与「用户选定前不得写任何章节正文」的停等闸门、18 项可数硬失败与完成标准数字表。
v4 的决定性教训是：**可数指标可以被优化到全绿而质量并不改善**，前向验证才是唯一验收。

**v6 保留并新写的**：身份与语体、照 12 篇三大刊实测搭骨架、9 条禁区（第 9 条
「把作业过程写进正文」为 08-20 新增）、五条不能破（第 5 条「制度建议要嫁接，不要发明」为
08-20 新增；第 4 条补入「结论进正文，出处、样本与限度进脚注」）、引注可靠性双口径、
用户已给详细大纲时结构指标降为参考。护栏要求的五节规程外壳（触发条件／开工前检查／
用户决策点／完成标准／最终报告）为上线时新补，**开工前检查明写「读文献，再动笔，
不许凭题目猜结构」**。

**伴随材料换位**：`read_workflow` 的自动附带由《成文技艺手册》改为
《论文初稿（少年司法版）—三大刊结构.md》。手册常量保留在出厂模板中（用户机器上的文件不删），
顶部加停用戳，不再附带。

**验证方式**：`check_guides.py` 全部通过（含 ③ 六条工作流一致、④b 出厂模板指纹登记）；
`src/tests` 全量通过，其中 11 项契约测试由 v4 口径改写为 v6 口径。

**仍未做的**：v6 装进出厂模板后**尚未实机跑过一次完整启动**，也未用它在正式安装里真写一篇；
E/F/G 三稿都是直接 Read 开发仓库文件跑出来的。上线后的第一篇仍应视为前向验证。
