# PaperPiggy 运行时瘦依赖（无 torch/optimum；嵌入+重排走 onnxruntime 或 API）
# 传递依赖(numpy/pyarrow/pandas/scipy 等)会自动装。
#
# 可复现构建：本文件用「区间」防上游大版本把 bundle 拉崩；正式出包前建议冻结一份精确锁：
#     python -m pip freeze > requirements.lock
# 并随 bundle 一起走、下次按 requirements.lock 重建，杜绝“几个月后重建就崩”。
fastapi
uvicorn
lancedb>=0.33
onnxruntime>=1.17,<2        # 依赖 VC++ 运行库、ABI 敏感，跨大版本易崩 → 锁主版本
transformers>=5,<6         # 仅用其 tokenizer 加载；需与 tokenizers 版本匹配
tokenizers>=0.22,<0.23     # 与 transformers 的 ABI 绑定，别让其自由漂移
                           # ↑ 上限对齐 requirements.lock 里已实机验证的 5.13.0 / 0.22.2。
                           #   曾经写成 <5 / <0.22，与实装矛盾——照着重装会得到一个
                           #   本项目从未跑过的组合。改这两行前先看 requirements.lock。
bm25s
jieba
scikit-learn
pypdfium2                  # PDF 取文本（BSD-3/Apache-2.0）。
                           # ⚠️ 不要换回 pymupdf / pymupdf4llm：AGPL + Polyform Noncommercial，
                           #    与本项目 Apache-2.0 冲突，且中文标点会错序。理由见 extract.py 顶部。
rapidocr>=3.9,<4           # 空白 PDF 页本地 OCR；3.9+ 默认随包带 PP-OCRv6 small ONNX 模型。
python-docx>=1.1           # 综述导出 Word（缺它则导出恒降级为 .md，且不报错）
requests
pywebview
# 桌面窗口后端按平台分：Windows 走 WebView2，靠 pythonnet(.NET 桥)；macOS 走 Cocoa/WebKit，靠 pyobjc。
# ⚠️ pythonnet 在 macOS 上需要 .NET/Mono、pip 装常直接失败——所以限定 win32，别让它拖垮 mac 的安装。
#    Linux 若要跑还需 pywebview[gtk] 或 [qt] 及对应系统库，另行处理。
pythonnet ; sys_platform == "win32"
pyobjc-framework-WebKit ; sys_platform == "darwin"
pyobjc-framework-Cocoa ; sys_platform == "darwin"
